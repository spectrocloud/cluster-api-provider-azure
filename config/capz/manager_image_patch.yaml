apiVersion: apps/v1
kind: Deployment
metadata:
  name: controller-manager
  namespace: system
spec:
  template:
    spec:
      containers:
      # Change the value of image field below to your controller image URL
      - image: gcr.io/spectro-dev-public/devop2023/release-fips/cluster-api-azure-controller:v1.3.2-spectro-4.0.0-dev
        name: manager

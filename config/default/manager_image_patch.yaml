apiVersion: apps/v1
kind: Deployment
metadata:
  name: controller-manager
  namespace: system
spec:
  template:
    spec:
      containers:
      # Change the value of image field below to your controller image URL
      - image: gcr.io/spectro-dev-public/cluster-api-azure-controller-amd64:lochan-dev-1
        name: manager

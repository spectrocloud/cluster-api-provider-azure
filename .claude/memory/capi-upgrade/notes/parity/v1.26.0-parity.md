# CAPZ → v1.26.0 — fork behavior & parity map (Gate-1 input)

Generated 2026-07-15 from: JIRA tickets (18), GitHub PR descriptions+comments (23 PRs), per-commit
diffs vs the v1.26.0 tree, and the round-2 plan record (`runtime/20260715-175523-PLAN-capz.md`).
Per-commit detail: `notes/commits/<sha>.md` (all 109 plan commits). Cluster deep-dives:
`notes/parity/clusters/*.md`. **Advisory — decisions land via the ledgers + human approval.**

## The five functionality clusters

| cluster | commits | verdict |
|---|---|---|
| aks-managed-clusters | 22 | Mostly UPSTREAMED (auto-upgrade, outboundType, disableLocalAccounts, OsSKU converter, ServiceCIDR fix all in v1.26.0). Real carries: **enableAzureRBAC (24708cad, HIGH)**, FqdnSubdomain slice of 15ec55de, SP-auth warning suppression (9972a7a1), 2 webhook hunks re-targeted to `internal/webhooks/`. |
| cloud-environments | 35 | **FORK-ONLY must-carry**: the AzureSecret env stack (PEM-7501: JSON→ConfigMap→mounted→main.go loads env; location mapping ussec→eastus2; SetEnvironment race lock). Land the #117 chain as ONE net-diff unit. Gov-cloud root 70464cd1 is UPSTREAMED (kubernetes-sigs#3509 → in v1.10.0+); the v1.18-rebase repair megacommits are SUPERSEDED. |
| networking | 22 | 3 must-carry units: private-DNS FQDN chain (PEM-483/484 net: 3-arg GeneratePrivateFQDN, conditional on custom zone), private-cluster LB-IP cascade (dynamic-IP read-back), PR-125 VM-deletion verification. Plus SubnetAll role (5fb2ad0a). 7 rows upstreamed/dead. |
| webhook-runtime-infra | 19 | Must-carry core: split-pod main.go wiring (webhook-port default 0 — **the most load-bearing line in the fork**), PEM-2613 TLS pin, `--metrics-bind-addr` flag, gallery/VNET-RG admission relaxations (re-target to `internal/webhooks/`), spectro generator inputs, FIPS build chain, spectro-release.yaml (Patch-H interface). controller-runtime merge 94233eeb SUPERSEDED. |
| cve-build-noise | 11 | All SKIPs verified safe (go.mod/go.sum/Dockerfile/workflow only; c9809113 upstreamed as f4efea5f). 2 PICKs (spectro-release.yaml go pins) carry clean. |

## ⚠️ Regression traps found (resolver/reviewer MUST honor)

1. **validateAPIServerLB immutability (networking, HIGH):** v1.26.0 adds unconditional internal-LB
   private-IP immutability + `privateIPCount != 1` checks. The fork's dynamic-IP read-back updates
   spec `""` → assigned IP; upstream's check would wedge every dynamic private cluster. Carry the
   fork's Static-conditioned variant.
2. **azure.ReconcileAll refactor (networking):** v1.26.0 loadbalancers.Reconcile discards created
   resources — the fork's read-back loop must be re-inlined.
3. **Dead-path trap (webhooks, HIGH):** e3c6f868/45d9bb84 apply cleanly to `api/v1beta1/*_webhook.go`
   — files v1.26.0 DELETED. A "clean pick" lands dead code while live `internal/webhooks/` stays
   strict → silently loses the dual-gallery + VNET-RG relaxations. Re-target as PICK-RESOLVE.
4. **93449d5c would break the build:** picking creates an empty `.go` file in `exp/api/v1beta1/`.
5. **b3a9f3b5 `…ForStandard` bodies are frozen v1.18 copies (cloud-env, HIGH):** carrying verbatim
   reverts 8 releases of upstream LB/publicIP fixes for ALL clusters. Regenerate from v1.26.0
   implementations; keep only the AzureSecret guards.
6. **enableAzureRBAC (24708cad, HIGH):** v1.26.0 still hardcodes `EnableAzureRBAC: AADProfile.Managed`
   (managedcontrolplane.go:595). Dropping flips AAD clusters K8s-RBAC → Azure-RBAC (PCP-6412 / PWC).
7. **ASO hub moved** to `v1api20250801/storage` — carried code/tests pinned to `v1api20240901`
   won't compile. Fork's ASO-hub import changes: do not carry.
8. **604a2909 drift:** AMCP/AMMP webhook removal lives only as a hand-edit to generated
   `config/webhook/manifests.yaml`; `make generate` restores them. If intended, re-implement as a
   spectro/global kustomize delete-patch. **Human call needed.**

## Recommended ledger changes (pending reviewer/human application — NOT yet applied)

Decision flips (evidence in the per-commit notes):
- → SKIP: 8b5ab295, 16ca18d3, aef95617, 1c224e7f, 70464cd1(+downgrade regwatch H), f51b1c5e(+downgrade regwatch H), 94233eeb, 93449d5c (currently PICK — build-breaker), 35e14baa/70b75f13 (already SKIP — verified)
- → PICK-RESOLVE (re-target to moved files): e3c6f868, 45d9bb84, 248e1eb1-unit (5e819cea, f9cfc76b, ad0f208f main.go slice)
- → land-as-unit via net diff: #117 AzureSecret chain, private-DNS chain (a8ff143f/adb3f75a/72906f59/e176e7db), location-mapping unit (e8b25112/32e8a1f1/2d63d1f9/363e2980), PR-125 chain
- Regression Watch ADD (absent today): e3c6f868, 45d9bb84, 24708cad
- Gate-1 question for Palette: PCP-1467 userAssignedIdentities plumbing is functionally dead on the fork (scope populates, spec never consumes) — wire or drop?

## F4_HOTSPOTS (conf) — evidence-backed

`main.go · azure/defaults.go · azure/scope/cluster.go · azure/scope/managedcontrolplane.go ·
azure/services/managedclusters/ · azure/services/loadbalancers/ · azure/converters/ ·
api/v1beta1/*webhook* · exp/api/v1beta1/ · azure/scope/azure_secret_cloud.go`

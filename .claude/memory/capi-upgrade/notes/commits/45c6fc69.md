# 45c6fc69 — PCP-6734 Fix go vulns
- **cluster:** cve-build-noise · **plan decision (r2):** SKIP
- **tickets:** PCP-6734 — same ticket as `d0610cb2` ("Update go version (1.26.3) and rebuild images"; batch3: "build-infra only; superseded by target go + Patch-G gate. Commits SKIPped").
- **PR:** #151 "PCP-6734 Fix go vulns" (merged 2026-05-19T07:28:59Z, ~1h25m after #150 same day, no body/comments recorded) — follow-up PR under the same ticket, closing vulns the go-version bump alone didn't cover.
- **behavior:** `go.mod`/`go.sum` dependency bumps: `cel.dev/expr` 0.22.1→0.25.1; `golang.org/x/oauth2` 0.28.0→0.34.0; `google.golang.org/genproto/googleapis/{api,rpc}` and `google.golang.org/grpc` 1.71.0→1.79.3, `google.golang.org/protobuf` 1.36.6→1.36.10 (all indirect, gRPC/protobuf toolchain family). Contract: close CVEs in the gRPC/protobuf/CEL/oauth2 indirect-dependency chain pulled in transitively (likely via controller-runtime/k8s client-go deps).
- **pr/ticket context:** ticket batch3 note frames the whole PCP-6734 pair (`d0610cb2` + `45c6fc69`) as build-infra-only and already SKIPped; no distinguishing PR comments recorded.
- **parity vs v1.26.0:** SUPERSEDED — target's dependency floor (go 1.25.0+, later pushed to 1.26.4 within this same cluster) will carry its own current gRPC/protobuf/oauth2 versions, and the Patch-G govulncheck rescan on the new branch re-verifies this indirect chain from scratch.
- **resolution guidance:** Confirmed touches only `go.mod`/`go.sum`. Safe to SKIP — no functional/API-surface change, purely indirect transitive-dependency version bumps in the observability/RPC toolchain that the CVE gate re-derives independently.

# 79f5be32 — PCP-6137 Handle CAPZ vulenerability
- **cluster:** cve-build-noise · **plan decision (r2):** SKIP
- **tickets:** PCP-6137 — "Handle CAPZ vulnerability" (Sub-task, Resolved). Per batch3 JIRA context: "No description. Pure CVE-bump work on the fork (go.mod/go.sum). Commits already SKIPped (superseded by target deps + CVE gate)."
- **PR:** #139 "PCP-6137 Handle CAPZ vulenerability" (merged 2026-03-05T09:45:09Z, no body/comments recorded) — same PR # as `d3e72af4` (see below), i.e. PR #139 was landed as two separate commits ~2 minutes apart.
- **behavior:** Bumps `go.mod`/`go.sum`: `go 1.23.8` → `go 1.24.0` (+ adds `toolchain go1.24.3`); `github.com/go-logr/logr` 1.4.2→1.4.3; `go.opentelemetry.io/otel` and `.../otel/trace` 1.35.0→1.40.0; `go.opentelemetry.io/auto/sdk` 1.1.0→1.2.1; `go.opentelemetry.io/otel/metric` 1.35.0→1.40.0 (indirect). Contract: close whatever CVE(s) were flagged against the go 1.23.x toolchain / OTel 1.35.x line at the time.
- **pr/ticket context:** ticket has no description of specific CVE IDs; JIRA batch3 explicitly frames this as pure dependency-bump work with no functional ask.
- **parity vs v1.26.0:** SUPERSEDED — target floor is far ahead (this same cluster's later commits push go to 1.25.0 then 1.26.4; OTel 1.40.0 is itself superseded by whatever the v1.26.0 branch pins, and will be re-verified fresh by govulncheck regardless).
- **resolution guidance:** Confirmed touches only `go.mod`/`go.sum` (`git show --name-only` output: `go.mod`, `go.sum`). Safe to SKIP — the target's own dependency floor plus the Patch-G CVE gate (govulncheck → bump → tidy → rebuild → rescan) is a strict superset of what this commit closed. No 3-way resolution needed.

# fc3e22b4 — add autobackport
- **cluster:** webhook-runtime-infra · **plan decision (r2):** NEEDS-DECISION (conflict on .gitignore)
- **tickets:** none
- **PR:** #141 "chore: add autobackport" (Kun483, merged 2026-04-06; no body/comments)
- **behavior:** Adds `.github/workflows/backport.yaml` — on merged PRs labeled `auto-backport`, runs sorenlouv/backport-github-action v9.5.1 with label prefix `backport-` to open backport PRs to release branches (e.g. `spectro-release-4.9`) automatically, preserving reviewers. Also appends `vendor/` to `.gitignore`.
- **pr/ticket context:** title only.
- **parity vs v1.26.0:** **FORK-ONLY must-carry.** Upstream has no such workflow. The workflow file is additive (no collision); only `.gitignore` conflicts because upstream's `.gitignore` evolved — trivial one-line union.
- **resolution guidance:** Resolve `.gitignore` by union (keep upstream v1.26.0 content + add `vendor/`). Note upstream v1.26.0 `.gitignore` may already cover vendor/ — check; if so the hunk is a no-op and only the workflow file carries. Safe to force-PICK with the trivial resolution; no regression surface.

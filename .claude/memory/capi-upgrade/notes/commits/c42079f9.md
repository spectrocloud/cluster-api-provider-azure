# c42079f9 — Adding enhanced logic for checking the vm deletions
- **cluster:** networking · **plan decision (r2):** PICK-VERIFY
- **tickets:** PCP-5171 (from PR title: azure machine deletion reports failure due to error500 but deletion actually succeeded)
- **PR:** #125 "Pcp 5171 azure machine deletion reports failure due to error500 but actually deletion succeeded" (merged 2025-10-03, author HigashikataZhangsuke/YinzheZhang; empty body, no review comments)
- **behavior:** First draft of the PR #125 fix, at the WRONG layer: puts 5xx-tolerant VM-deletion verification into `azure/services/async/async.go` `DeleteResource` (+ adds `azure.IsTransientServerError` for azcore.ResponseError 500/502/503/504). Motivation: Azure US Secret Cloud returns 500 on VM delete even when deletion succeeded, wedging AzureMachine deletion.
- **pr/ticket context:** PR body empty; intent from title + author overlap with PEM-7501 (Azure Secret Cloud support, same author).
- **parity vs v1.26.0:** FORK-ONLY must-carry — but ONLY as part of the 4-commit NET (see 4693ac4e). The async.go changes introduced here are fully retracted by 4f838de6+9c0457e2 (net async.go delta across the chain = 0). Only `azure/errors.go::IsTransientServerError` survives from this commit.
- **resolution guidance:** Apply the PR #125 chain as a unit (or squash to NET = errors.go helper + azuremachine_reconciler.go changes). Never pick c42079f9 alone — it would leave transient-error swallowing inside generic async DeleteResource, affecting EVERY service's deletes, which the PR itself walked back.

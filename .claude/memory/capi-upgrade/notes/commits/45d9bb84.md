# 45d9bb84 — Change AzureCluster Webhook Validation for Shared & Compute Gallery
- **cluster:** webhook-runtime-infra · **plan decision (r2):** PICK (heuristic "applies cleanly, fork-only")
- **tickets:** none referenced
- **PR:** none — direct push (Vishwanath Taykhande, May 2025)
- **behavior:** Final shape of the gallery relaxation (supersedes 6d749add): in `validateSingleDetailsOnly`, SharedGallery and ComputeGallery become an **else-if pair — ComputeGallery wins when both are set** — instead of two independent checks that each flag "already found image details". Contract: palette may submit an Image with BOTH `sharedGallery` and `computeGallery` populated (mixed-version worker pools during palette upgrade, avoiding node repave) and the webhook must accept it; Marketplace/ID combinations stay rejected. In-diff comment: "can be reverted back … once all clusters from palette have been upgraded" — that revert never happened, so this is live behavior.
- **pr/ticket context:** none; behavior inferred from diff comments.
- **parity vs v1.26.0:** **FORK-ONLY must-carry — and the plan's PICK is a trap.** v1.26.0 has NO `api/v1beta1/azureimage_validation.go`; the live copy is `internal/webhooks/azureimage_validation.go`, whose `validateSingleDetailsOnly` (lines 52-85) still enforces strict single-gallery. A "clean" pick recreates the api/v1beta1 file as dead code while the live webhook rejects palette's dual-gallery images → **create/scale/upgrade failures or forced node repave** on every cluster where palette sets both galleries. Repave-class silent regression.
- **resolution guidance:** Re-target the else-if structure into `internal/webhooks/azureimage_validation.go::validateSingleDetailsOnly` (mechanical: replace the two independent `if image.SharedGallery`/`if image.ComputeGallery` blocks with the fork's `if ComputeGallery … else if SharedGallery …` form and carry the explanatory comment). Do NOT let the pick create the api/v1beta1 file. Note the same file's AzureMachine/AzureMachinePool webhooks all funnel through this one function, so one edit covers all kinds. Add unit test: image with both galleries → no error; gallery+Marketplace → error. Recommend PICK → NEEDS-DECISION/PICK-RESOLVE with re-target, and add to Regression Watch (currently absent).

# c95a3413 — Fix Vishu's comment
- **cluster:** cloud-environments (#117 chain, tail) · **plan decision (r2):** NEEDS-DECISION (conflict: main.go)
- **tickets:** PEM-7511
- **PR:** #117
- **behavior:** Gates the REST-config tuning behind the environment check: `if os.Getenv("AZURE_ENVIRONMENT") == azure.AzSecretCloudName { restConfig.Timeout=30s; QPS=20; Burst=30 }` — public-cloud deployments keep upstream client defaults; only AzSecret (slow, proxied, air-gapped endpoints) gets longer timeouts. This is the exact surviving main.go hunk at S (line ~333).
- **pr/ticket context:** direct fix for vishu2498's "move these configurations in the azure secret cloud environment only".
- **parity vs v1.26.0:** **FORK-ONLY must-carry.** v1.26.0 main.go has no such block. Note the env var `AZURE_ENVIRONMENT` is set by the palette-side deployment (not in this repo's manifests).
- **resolution guidance:** Apply the guarded block after `ctrl.GetConfigOrDie()` in v1.26 main.go (upstream main.go churned ±147 F..T — locate the restConfig creation point). Requires `azure.AzSecretCloudName` from defaults.go to land first.

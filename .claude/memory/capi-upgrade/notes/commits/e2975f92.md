# e2975f92 — PCP-1467: UserAssignedIdentities Fix
- **cluster:** aks-managed-clusters · **plan decision (r2):** NEEDS-DECISION
- **tickets:** PCP-1467 — "aks pack values userAssignedIdentities are not getting reflected in azure managed cluster" (Bug, Done): AMCP userAssignedIdentities must flow into the Azure ManagedCluster create/update call.
- **PR:** none — direct push (Vishwanath Taykhande, 2023-07-12)
- **behavior:** Makes aece2202's field actually take effect (SDK era): when `spec.userAssignedIdentities` is set, the ManagedCluster identity becomes `type: UserAssigned` keyed by `userAssignedIdentities[0].ProviderID` (single identity — first entry wins); otherwise SystemAssigned. Also forces the identity onto the update path and into computeDiff normalization so an identity change triggers a PUT.
- **pr/ticket context:** PCP-1467 confirms the user-visible contract: the UAI given in the AKS pack must appear on the launched managed cluster.
- **parity vs v1.26.0:** SUPERSEDED-as-dead on the fork / FORK-ONLY vs upstream. At 0164d80e none of this survives on the live path: `s.UserAssignedIdentities` is never read in `spec.go` Parameters — identity comes exclusively from upstream's `spec.identity` via `getIdentity()` (which supports `UserAssigned` + `UserAssignedIdentityResourceID`, same single-identity semantics). Upstream v1.26.0 identical (`getIdentity`, spec.go:691+).
- **resolution guidance:** Same Gate-1 decision as aece2202 (treat aece2202 + e2975f92 + efd40404 as one unit): PCP-1467's contract is currently satisfied — if at all — by Palette using `spec.identity`, NOT by this fork code. Verify Palette's AKS pack: if it still emits `userAssignedIdentities`, the contract is broken TODAY and the right fix at v1.26.0 is mapping the field to `Identity{Type: UserAssigned, UserAssignedIdentityResourceID: uai[0].ProviderID}` in the scope (few lines), or migrating the pack. Do not carry the SDK-era hunks.

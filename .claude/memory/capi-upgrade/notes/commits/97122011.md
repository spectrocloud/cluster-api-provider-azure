# 97122011 — Update Go version in spectro-release workflow to 1.24.12
- **cluster:** cve-build-noise · **plan decision (r2):** PICK
- **tickets:** none
- **PR:** #130 "Update Go version in spectro-release workflow to 1.24.12" (merged 2026-01-27, no body/comments recorded)
- **behavior:** Adds `GO_VERSION: 1.24.12` and `BUILDER_GOLANG_VERSION: 1.24.12` env vars to both the legacy-registry and FIPS build steps of `.github/workflows/spectro-release.yaml`, pinning the Go toolchain used to build the Spectro release images. Contract to preserve: the fork's release CI must pin an explicit, current Go toolchain version for its image builds (previously these env vars were absent/defaulted elsewhere).
- **pr/ticket context:** none — PR body empty, no review comments recorded; behavior inferred from diff only.
- **parity vs v1.26.0:** FORK-ONLY must-carry (mechanically) — `.github/workflows/spectro-release.yaml` does not exist upstream at all (confirmed: `git show v1.26.0:.github/workflows/spectro-release.yaml` → "exists on disk, but not in 'v1.26.0'"). This is Spectro-only release CI; upstream has no equivalent workflow to supersede or conflict with it.
- **resolution guidance:** Confirmed touches only `.github/workflows/spectro-release.yaml` (no .go files) — carries cleanly onto the new branch with zero conflict surface since upstream never had this file. The specific `1.24.12` value itself is transient: it's immediately superseded by the next commit in this same cluster (`ae875a02` → 1.24.13) and ultimately by whatever version the Patch-G CVE gate / P3c build-vars refresh pins at execute time. PICK the mechanism (the env-var block existing in the workflow), not the literal version string.

# 1c224e7f — Added subnetName spec to azure machine pool.
- **cluster:** networking · **plan decision (r2):** NEEDS-DECISION (MIXED generated+code)
- **tickets:** none (no ticket/PR context; behavior inferred from diff only)
- **PR:** none — direct push
- **behavior:** Despite the subject, this commit carries NO functional code: it regenerates the AzureManagedMachinePool CRD (+ spectro/generated/core-global.yaml) to expose `subnetName` ("SubnetName selects the Subnet where the MachinePool will be placed") and deletes 2 blank lines in azure/services/managedclusters/spec.go. The exp/api file entries are 0-line rename artifacts from the fork's rebase history. The actual field/validation predate it in the fork tree (api/v1beta1/azuremanagedmachinepool_webhook.go `validateMPSubnetName`).
- **pr/ticket context:** none.
- **parity vs v1.26.0:** UPSTREAMED — v1.26.0 natively has `AzureManagedMachinePool.spec.subnetName` (types_class.go:399-400 `SubnetName *string`, CRD infrastructure...azuremanagedmachinepools.yaml:594-598 "SubnetName specifies the Subnet where the MachinePool will be placed. Immutable.", validation in internal/webhooks/azuremanagedmachinepool_validation.go).
- **resolution guidance:** Recommend downgrading NEEDS-DECISION → SKIP/REGEN: the only code delta is whitespace; the generated artifacts are re-produced by `make generate manifests` + vendorcrd-sync on the target branch. Nothing to hand-carry. Verify post-regen that `subnetName` appears in the managed-machinepool CRD (it will — it's upstream's own field/description now).

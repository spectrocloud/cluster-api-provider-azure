# 034b0daf — PEM-7501: Loaded custom azure environment from file
- **cluster:** cloud-environments · **plan decision (r2):** PICK (fork-only, clean)
- **tickets:** PEM-7501 — Azure Secret Cloud support in CAPZ: JSON models the env → ConfigMap → mounted → main.go loads it
- **PR:** #113 "PEM-7501: Loaded custom azure environment from file" (author Guy Ni)
- **behavior:** Creates fork-only `azure/scope/azure_secret_cloud.go`: package `init()` reads `AZURE_ENVIRONMENT_FILE`, parses it with `autorest/azure.EnvironmentFromFile`, and registers it via `azure.SetEnvironment("AzureSecretCloud", env)` — first step of letting CAPZ authenticate against an air-gapped/secret Azure cloud whose endpoints aren't hardcoded in the SDK.
- **pr/ticket context:** PR body: "Added function to load custom azure environment from file to support Azure Secret Cloud". Design per PEM-7501 description.
- **parity vs v1.26.0:** **FORK-ONLY must-carry (as cascade root).** Nothing upstream loads custom Azure environments. NOTE: this exact mechanism (env-var file path, then folder scan) is **superseded intra-fork** by the #117 chain (f918f3cf switches to ConfigMap `azure-capz-env-config` + `InitializeAzureConfigForCluster`); at S tip no `AZURE_ENVIRONMENT_FILE/FOLDER` reference remains.
- **resolution guidance:** Safe PICK (new file, no target conflict). It is a stepping stone: correctness only holds if the #117 chain (through f1f1ebc2) lands on top, converging the file to the S-tip state (`git show 0164d80e:azure/scope/azure_secret_cloud.go`). If the #117 chain is instead applied as a net diff, this commit can be folded in.

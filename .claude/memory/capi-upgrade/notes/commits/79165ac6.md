# 79165ac6 — Ignore error for other future types in delete functionality
- **cluster:** networking · **plan decision (r2):** SKIP (empty on cumulative tree — duplicate)
- **tickets:** none (no ticket/PR context)
- **PR:** none — direct push
- **behavior:** EMPTY commit in the linearized fork history (0 files). Original content (twin ae04b30524, Apr 2023) refined 2c9ba2eb's fix in old async.go: tolerate errors from `isDone` checks on completed non-delete futures, log instead of fail, always clear finished long-running-operation state, and narrow the delete-path bail-out to `err != nil && future.Type == DeleteFuture`.
- **pr/ticket context:** none.
- **parity vs v1.26.0:** SUPERSEDED architecturally — same story as 2c9ba2eb: the patched processOngoingOperation/futures model was replaced by track2 pollers with per-future-type state (already in the v1.18 fork base). v1.26.0 async.go clears state after completed ops regardless of error.
- **resolution guidance:** SKIP safe: empty diff; target design eliminates the cross-future-type interference the fix worked around.

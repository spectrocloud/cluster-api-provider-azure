# faa067a7 — Spectro CICD integration
- **cluster:** webhook-runtime-infra · **plan decision (r2):** PICK (heuristic, fork-only)
- **tickets:** none
- **PR:** none — direct push (Feb-2025 re-fork base commit)
- **behavior:** Adds `.github/workflows/spectro-release.yaml` — the Spectro Release GH Action that builds and publishes the CAPZ controller images. Inputs: `release_version`, `rel_type` (release|rc); tags `v<version>-spectro`; pushes to `us-docker.pkg.dev/palette-images/palette/cluster-api-azure` (+ `palette-images-fips` FIPS variant; rc → spectro-images dev registries); guards on tag pre-existence; builds via remote docker over SSH. This workflow is the **Patch-H image-build interface**: the pipeline's IMAGE stage is literally `gh workflow run spectro-release.yaml --ref spectro-v<target>-master`.
- **pr/ticket context:** none. Later fork commits keep it alive (97122011 / ae875a02 bump its Go version — PCP-6027), confirming it is the live release path.
- **parity vs v1.26.0:** **FORK-ONLY must-carry.** Upstream has no spectro-release workflow. No upstream path collides (`.github/workflows/` fork-additive).
- **resolution guidance:** PICK is right. Two hygiene items at execute: (1) drop the `.github/.DS_Store` and `.github/workflows/.DS_Store` binaries this commit also adds; (2) pick the S-tip state of the workflow (97122011/ae875a02 already bump BUILDER_GOLANG_VERSION; the v1.26.0 branch needs go 1.25.x+ per target go.mod — reconcile with the Patch-G go-version outcome rather than keeping 1.24.x).

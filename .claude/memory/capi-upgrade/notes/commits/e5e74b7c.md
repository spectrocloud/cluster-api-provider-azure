# e5e74b7c — Decouple location mapping patch from #119, Only fix ticket for this PR
- **cluster:** cloud-environments (#121) · **plan decision (r2):** SKIP (empty on cumulative tree)
- **tickets:** PEM-5484 (branch); references PR #119
- **PR:** #121
- **behavior:** Scope-narrowing revert: removes the location-mapping code (machinepool.go -16, managedcontrolplane.go -13, util/azure -19, tests -42) from the #121 branch so that PR only carries the env-checking/cluster.go recovery; the mapping was to be re-merged separately (#119 → re-opened as #122 = 363e2980, an exact re-add).
- **pr/ticket context:** PR #122 body: "A copy of #119. When merging #121 it was marked as merged, while not actually."
- **parity vs v1.26.0:** **Net-zero (revert pair with 363e2980).**
- **resolution guidance:** SKIP is correct — but ONLY together with its context: the engine found it empty because the e8b25112/32e8a1f1 conflicts weren't applied in rehearsal. If the location-mapping chain is force-picked as a unit (e8b25112 → 32e8a1f1 → e5e74b7c → 363e2980), either include this removal or apply the unit as a net diff — otherwise the mapping call sites get **double-applied** (e8b25112's unguarded version + 363e2980's guarded version coexisting).

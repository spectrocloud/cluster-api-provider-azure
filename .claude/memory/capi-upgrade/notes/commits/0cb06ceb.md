# 0cb06ceb — Validation disabled for AzureManagedControlPlane
- **cluster:** aks-managed-clusters · **plan decision (r2):** NEEDS-DECISION
- **tickets:** none
- **PR:** none — direct push (Snehal Amrutkar, 2022-07-13)
- **behavior:** Original 2022 intent: disable AMCP admission validation so Palette could create AKS clusters with spec shapes upstream's webhook rejected. In the rebased spectro-master history the commit has degenerated to: (a) a stray EMPTY file `exp/api/v1beta1/azuremanagedcontrolplane_webhook.go` (0 bytes, later removed — not present at 0164d80e), (b) RBAC additions in `spectro/generated/core-global.yaml` (clusters get/list/watch + azuremanagedclusters/azuremanagedcontrolplanes `*` on the capz-aad-pod-id-nmi-role), (c) `commonLabels: cluster.x-k8s.io/provider: infrastructure-azure` in `spectro/global/kustomization.yaml`.
- **pr/ticket context:** no ticket/PR context; behavior inferred from diff only.
- **parity vs v1.26.0:** SUPERSEDED. The "validation disabled" behavior is NOT in effect on today's fork — `spectro/generated/core-global.yaml` at 0164d80e registers `validation.azuremanagedcontrolplanes.infrastructure.cluster.x-k8s.io` (line ~84667). The surviving payload is spectro deployment-manifest plumbing, which Patch F regenerates via vendorcrd-sync, and fork-local kustomize overlay config.
- **resolution guidance:** Do NOT try to re-disable AMCP validation at v1.26.0. Treat the spectro/generated hunk as REGEN (vendorcrd-sync owns it); keep the `spectro/global/kustomization.yaml` commonLabels line if the spectro overlay is carried as-is. The genuinely surviving "relaxed validation" fork intent lives in the two webhook tweaks captured in the cluster summary (missing-owner-Cluster tolerance + empty-VNet-RG upgrade path) — those must be re-applied in v1.26.0's `internal/webhooks/azuremanagedcontrolplane_webhook.go` (webhooks moved out of `api/v1beta1/` upstream).

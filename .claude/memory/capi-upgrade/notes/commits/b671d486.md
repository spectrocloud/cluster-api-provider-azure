# b671d486 — fix webhook issue (#85)
- **cluster:** webhook-runtime-infra · **plan decision (r2):** SKIP (empty on cumulative tree)
- **tickets:** none on the commit (part of the PCP-1595/1596 DNSPrefix work stream)
- **PR:** #85 "fix webhook issue" (no pr-85.json context file provided)
- **behavior:** EMPTY commit in the replayed history. Original content (pre-replay 523ee287a7): make the AMCP update-webhook immutability checks **nil-safe** for `DNSPrefix`, `DockerBridgeCidr`, `FqdnSubdomain` — compare with `pointer.StringDeref(x, "")` instead of raw pointer compare, so old clusters that stored `nil` don't get rejected when the field is later defaulted to `""`/set (false "immutable field changed" on upgrade).
- **pr/ticket context:** PR #85 title only; behavior recovered from the pre-replay original diff (523ee287a7).
- **parity vs v1.26.0:** **UPSTREAMED.** v1.26.0 `internal/webhooks/azuremanagedcontrolplane_webhook.go` lines 121-133 implement the nil-tolerant DNSPrefix immutability dance ("Normally, the defaulting webhooks will always set the new DNSPrefix…" with explicit `oldDNSPrefix == nil` handling) — same code as at S. DockerBridgeCidr was removed from the API upstream; FqdnSubdomain follows the same nil-aware pattern.
- **resolution guidance:** SKIP is safe — commit is empty and the intent is present in the target. Nothing to verify beyond the existing AMCP webhook unit tests at T1.

# b3215651 — PEM-7501: Changed to load all json files instead of a single file for Azure Environments
- **cluster:** cloud-environments · **plan decision (r2):** PICK (fork-only, clean)
- **tickets:** PEM-7501 (see 034b0daf)
- **PR:** #113
- **behavior:** Generalizes 034b0daf: reads `AZURE_ENVIRONMENT_FOLDER` and loads EVERY `*.json` in it as a named Azure environment (env.Name from the JSON), instead of a single hardcoded "AzureSecretCloud" from one file — supports multiple custom clouds mounted from one ConfigMap volume.
- **pr/ticket context:** reviewer snehala27 later asked (on #117) "What will happen in the cases where AZURE_ENVIRONMENT_FOLDER was used. This no more exists now" — confirming the folder mechanism was consciously retired in #117.
- **parity vs v1.26.0:** **FORK-ONLY (superseded intra-fork).** Folder scanning is gone at S tip; replaced by ConfigMap JSON → temp file → `EnvironmentFromFile` in `processAzureEnvironmentJSON`.
- **resolution guidance:** Safe PICK as cascade step on the fork-only file; final state converges via the #117 chain. No shared-file exposure.

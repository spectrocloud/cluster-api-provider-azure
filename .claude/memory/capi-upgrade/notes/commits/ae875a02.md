# ae875a02 — PCP-6027 Update go version in spectro release yaml (#132)
- **cluster:** cve-build-noise · **plan decision (r2):** PICK
- **tickets:** PCP-6027 — "Update go version on CAPI/CAPx controllers (critical CVE-2025-68121, go 1.24.12)" (Task, Resolved); builder image bump to go 1.24.13 across all CAPx controller images incl. azure v1.18.0-spectro-4.8.0. Per batch2 JIRA context: "build-infra only; superseded by target go 1.25.0 + Patch-G CVE gate on the new branch."
- **PR:** #132 "PCP-6027 Update go version in spectro release yaml" (merged 2026-02-23, no body/comments recorded)
- **behavior:** Bumps the same two env-var pairs introduced by `97122011` from `1.24.12` → `1.24.13` in both build steps of `.github/workflows/spectro-release.yaml` (`GO_VERSION` and `BUILDER_GOLANG_VERSION`), directly chaining off that prior commit. Same contract as `97122011`: pin an explicit, current Go toolchain for the fork's release-image builds, now specifically to remediate CVE-2025-68121.
- **pr/ticket context:** ticket names the exact CVE (CVE-2025-68121) driving the bump. PR body/comments empty in the recorded context.
- **parity vs v1.26.0:** FORK-ONLY must-carry (mechanically) — same file as `97122011`, confirmed absent upstream at v1.26.0. No upstream equivalent to supersede or conflict.
- **resolution guidance:** Confirmed touches only `.github/workflows/spectro-release.yaml`; diff is a clean 4-line version-string bump on top of `97122011`'s change (`git show ae875a02 -p` shows only `1.24.12`→`1.24.13` on the same two lines). Carries cleanly, no conflict surface. As with `97122011`, the literal `1.24.13` value is transient and immediately made moot by later commits in the cluster and by the Patch-G CVE gate / P3c build-vars refresh re-pinning at execute time — CVE-2025-68121 itself is closed by the target's own go 1.25.0+ floor plus the fresh govulncheck rescan, not by carrying this specific version string.

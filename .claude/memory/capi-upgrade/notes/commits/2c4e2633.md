# 2c4e2633 — Fix for Webhook Validations & ManagedCluster Spec Updation
- **cluster:** cloud-environments (previous-upgrade reconcile subgroup) · **plan decision (r2):** NEEDS-DECISION (conflict: managedclusters/spec.go, internal/webhooks/azuremanagedcontrolplane_validation.go)
- **tickets:** none
- **PR:** none — direct push (2025-04-22, v1.18 rebase repair)
- **behavior:** Repair of two fork AKS behaviors after the v1.18 replay: (a) `validateManagedClusterNetwork` becomes tolerant of a missing owner Cluster (wraps CIDR checks in `if err == nil` instead of hard InternalError) — fork behavior letting AMCP validation pass when the owner Cluster isn't readable; (b) restructures `ManagedClusterSpec.Parameters()` existing-object handling (comments out the upstream in-place diff block and re-lands the fork's ManagedCluster spec-updation flow — addon-profile merging, agent-pool preservation, provisioning-state gate). Both belong functionally to the AKS managedclusters feature cascade (a2b1a593/1c814218/e2975f92/efd40404/d8e1ce40).
- **pr/ticket context:** none
- **parity vs v1.26.0:** **SUPERSEDED as a patch; behavior FORK-ONLY.** The final S-tip `spec.go`/webhook state is the contract; this commit is only the repair step that produced it on the v1.18 base. v1.26.0 upstream `Parameters()` changed again (hub storage v1api20250801).
- **resolution guidance:** Do not apply standalone; resolve with the managedclusters cascade by diffing S-tip `azure/services/managedclusters/spec.go` + AMCP validation against v1.26.0 and re-applying the net fork hunks (owner-cluster-tolerant validation; existing-MC merge behavior). Watch the hub import: v1.26.0 uses `v1api20250801/storage` — never carry the fork's older hub version line.

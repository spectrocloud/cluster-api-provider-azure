# 601023f0 — private cluster change
- **cluster:** networking · **plan decision (r2):** NEEDS-DECISION (conflict: azure/services/loadbalancers/loadbalancers.go, internal/api/v1beta1/azurecluster_default.go)
- **tickets:** none (private-cluster feature family; no ticket/PR context — behavior inferred from diff)
- **PR:** none — direct push
- **behavior:** Root of the fork's custom API-server-internal-LB IP feature. Adds `LoadBalancerClassSpec.IPAllocationMethod` (defaults "Dynamic") and `PrivateIP`: user can pin a static private IP for the API-server internal LB or let Azure dynamically assign one, instead of upstream's hardcoded 10.0.0.100. After LB creation, `loadbalancers.Reconcile` reads back the actually-assigned frontend private IP and persists it into `APIServerLB().FrontendIPs[0].PrivateIPAddress`. Adds validation: API-server LB has exactly 1 frontend IP; internal LB cannot have a public IP; private IP effectively immutable when Static.
- **pr/ticket context:** none. Contains debug `fmt.Println` lines (removed later by 3d6a0310).
- **parity vs v1.26.0:** PARTIAL/SUPERSEDED-in-structure. Fields `IPAllocationMethod`/`PrivateIP` absent upstream (fork-only, must-carry). BUT: (1) v1.26.0 `loadbalancers.Reconcile` collapsed to `azure.ReconcileAll` (azure/reconcile.go:34) which DISCARDS the created resource — the read-back has no home; (2) defaulting moved to internal/api/v1beta1/azurecluster_default.go (free functions, `setDefaultAzureClusterAPIServerLB`); (3) validation moved to internal/webhooks/azurecluster_validation.go and upstream now itself validates 1-frontend-IP (public) + no-public-IP-on-internal + UNCONDITIONAL private-IP immutability.
- **resolution guidance:** Treat 601023f0+c7b9d887+b80d4dfe+3d6a0310+8c6bd5fa as one unit; write the fork's current NET state (spectro-master 0164d80e already armnetwork-ported): (a) re-add both fields to `LoadBalancerClassSpec` + regen CRD/deepcopy; (b) re-inline the fork's Reconcile loop in loadbalancers.go (do NOT use ReconcileAll) including the armnetwork read-back with nil guards; (c) port PrivateIP defaulting into `setDefaultAzureClusterAPIServerLB`; (d) in internal/webhooks validateAPIServerLB, RELAX upstream's unconditional internal-LB private-IP immutability — it would reject the dynamic read-back update (""→assigned IP) and brick private-cluster reconcile. Keep upstream's APIServerILB-feature-gate branches intact.

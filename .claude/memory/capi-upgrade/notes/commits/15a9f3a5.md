# 15a9f3a5 — windows support-added OSType to ammp and amcp
- **cluster:** aks-managed-clusters · **plan decision (r2):** NEEDS-DECISION
- **tickets:** none
- **PR:** none — direct push (Snehal Amrutkar, 2022-07-14)
- **behavior:** Original 2022 intent: Windows node-pool support — expose `osType` (Linux/Windows) on AzureManagedMachinePool so Palette can create Windows AKS pools. Rebased remnant: appends a LEGACY duplicate `AgentPoolSpec` struct (with `OSType *string`) to `azure/types.go`, plus a Makefile whitespace fix, empty webhook files, and kustomization tweaks.
- **pr/ticket context:** no ticket/PR context; behavior inferred from diff only.
- **parity vs v1.26.0:** UPSTREAMED (behavior) + dead code (payload). v1.26.0 AMMP has `Spec.OSType` with `DefaultOSType = LinuxOS` (`api/v1beta1/azuremanagedmachinepool_types.go:34-35`), webhook defaulting (`internal/webhooks/azuremanagedmachinepool_webhook.go:66-67`) and OSType validation; scope passes it through (`azure/scope/managedmachinepool.go:179`); the ASO converter copies `OsType` (`azure/converters/managedagentpool.go`). The fork's `azure/types.go` AgentPoolSpec duplicate is DEAD — no live references at 0164d80e (only a stale comment on `GetAllAgentPoolSpecs`); the real agent-pool spec is `azure/services/agentpools.AgentPoolSpec`.
- **resolution guidance:** SKIP-safe. Do not carry the `azure/types.go` struct onto v1.26.0 — it is dead code that only invites conflicts. Windows/OSType behavior is fully covered upstream.

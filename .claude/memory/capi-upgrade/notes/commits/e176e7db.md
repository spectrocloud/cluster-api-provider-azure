# e176e7db — fix private dns issue
- **cluster:** networking · **plan decision (r2):** PICK-VERIFY
- **tickets:** none on commit; belongs to PEM-483/PEM-484 chain
- **PR:** none — direct push
- **behavior:** Final link of the private-DNS chain: makes the cluster-name-in-FQDN scheme CONDITIONAL. `GeneratePrivateFQDN(zoneName, clusterName, isPrivateDNSZoneName)` returns `apiserver.<cluster>.<zone>` only when the user supplied a custom `PrivateDNSZoneName`; the default generated zone keeps upstream's `apiserver.<zone>` (the generated zone name already embeds the cluster name — unconditional application had produced a wrong/duplicated FQDN, which is why adb3f75a reverted the first attempt).
- **pr/ticket context:** none; inferred from diff + revert chronology.
- **parity vs v1.26.0:** FORK-ONLY must-carry (as the NET of the 4-commit chain). v1.26.0: `GeneratePrivateFQDN` still 1-arg (azure/defaults.go:138), `PrivateDNSSpec` hostname still constant (azure/scope/cluster.go:620), `APIServerHost` private branch still 1-arg call (azure/scope/cluster.go:1003).
- **resolution guidance:** NET state to preserve (== fork spectro-master today): (1) defaults.go `GeneratePrivateFQDN(zone, cluster string, isPrivateDNSZoneName bool)`; (2) cluster.go `getHostName()` → `apiserver.<cluster>` iff `Spec.NetworkSpec.PrivateDNSZoneName != ""`, used as `RecordSpec.Hostname` in `PrivateDNSSpec()`; (3) `APIServerHost()` private branch computes `isPrivateDNSZoneName = len(PrivateDNSZoneName)>0` and calls the 3-arg form. Watch two target-side changes: the `PrivateDNSZoneMode() != None` guard (59c26c982f) now wraps PrivateDNSSpec — keep fork edits inside it; and upstream 6e62f2cdd0 (= fork fc8f2606) adds `PrivateDNSZoneResourceGroup` to the same spec block — do not clobber it. Interop is coherent: custom zone in any RG + cluster-scoped hostname.

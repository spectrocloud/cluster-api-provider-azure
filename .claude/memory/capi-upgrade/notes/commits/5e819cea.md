# 5e819cea — logic to separate webhook and controller as separate pods
- **cluster:** webhook-runtime-infra · **plan decision (r2):** NEEDS-DECISION (conflict on main.go)
- **tickets:** none
- **PR:** none — direct push (Snehal Amrutkar, Jul 2022, replayed into the re-forked history)
- **behavior:** Companion of 248e1eb1 in the replayed history. Despite the subject, the replayed diff is tiny: fixes the `--webhook-port` help-text typo ("installed.." → "installed.") and adds startup log markers `setupLog.V(0).Info("registerControllers")` / `("registerWebhooks")` at the top of each register function — operator-visible confirmation of which mode a pod booted in. The actual split logic lives in 248e1eb1 (the replay flattened the original 2022 commit).
- **pr/ticket context:** none; behavior inferred from diff only.
- **parity vs v1.26.0:** **FORK-ONLY must-carry (as part of the 248e1eb1 unit).** v1.26.0 has neither the log markers nor (obviously) the fork help text.
- **resolution guidance:** File `main.go`. Treat 248e1eb1 + 5e819cea + f9cfc76b + ad0f208f as ONE unit and re-derive the net main.go delta on top of v1.26.0 rather than resolving each conflict in isolation (see cluster note webhook-runtime-infra.md for the exact net delta). For this commit only the two `setupLog.V(0).Info(...)` lines and the flag help string matter. Safe to fold into the 248e1eb1 resolution; do not spend a review round on it.

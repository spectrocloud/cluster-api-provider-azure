# 3f82514c — Fix for Subnets & package changes
- **cluster:** cloud-environments (previous-upgrade reconcile subgroup) · **plan decision (r2):** NEEDS-DECISION (conflict: api/v1beta1/types.go, loadbalancers.go, internal/api/v1beta1/azure{cluster,machine}_default.go)
- **tickets:** none
- **PR:** none — direct push (2025-04-17, v1.18 rebase repair)
- **behavior:** Build/behavior repair for the fork's **shared-subnet + private-LB** features (owned by the subnet/private-cluster cluster, e.g. 5fb2ad0a/601023f0/8c6bd5fa) after the v1.18 replay: (a) ports the private-LB frontend-IP readback in `loadbalancers.go Reconcile()` from the removed legacy SDK type `network.LoadBalancer` to `armnetwork.LoadBalancer` (reads `Properties.FrontendIPConfigurations[0].PrivateIPAddress` back into `APIServerLB().FrontendIPs[0]`); (b) comments out a duplicated `Name` field in `SubnetSpec` (v1.18 already has it via SubnetClassSpec — replay duplicated it); (c) subnet-defaulting fixes in azurecluster/azuremachine defaults.
- **pr/ticket context:** none
- **parity vs v1.26.0:** **SUPERSEDED as a patch; the underlying fork behavior is FORK-ONLY and lives on at S.** The armnetwork readback + subnet defaulting behavior survives at spectro-master tip; v1.26.0 does not have it. But THIS commit is only the SDK-migration form of it — replaying it standalone onto v1.26 double-applies half a fix.
- **resolution guidance:** Do not apply standalone. Fold into the private-LB/subnet cascade resolution: when 601023f0/3d6a0310/8c6bd5fa are resolved against v1.26.0, take the **net S-tip form** of `loadbalancers.go` readback and `internal/api/v1beta1/azure{cluster,machine}_default.go` subnet defaults (upstream moved defaults to internal/), then this commit is empty. SubnetSpec.Name hunk must NEVER be applied to v1.26 types.go (it would comment out a real field).

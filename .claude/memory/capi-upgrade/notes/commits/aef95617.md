# aef95617 — Updated the check for updating machinepool when using autoscalar (#42)
- **cluster:** networking · **plan decision (r2):** NEEDS-DECISION (conflict: azure/const.go)
- **tickets:** none
- **PR:** fork PR #42 (no context file captured; pr_refs only)
- **behavior:** Residual of the fork's pre-upstream autoscaler support: adds const `ReplicasManagedByAutoscalerAnnotation = "cluster.x-k8s.io/replicas-managed-by-autoscaler"` (the PRE-standardization spelling) and two `ManagedMachinePoolScope` helpers (`UpdateCAPIMachinePoolReplicas`, `UpdateCAPIMachinePoolAnnotations`). Original purpose: when AKS autoscaler manages an agent pool, reflect actual node count back into the CAPI MachinePool instead of scaling it back down.
- **pr/ticket context:** none captured.
- **parity vs v1.26.0:** SUPERSEDED + dead code. CAPI standardized on `cluster.x-k8s.io/replicas-managed-by` consumed via `annotations.ReplicasManagedByExternalAutoscaler` (v1.26.0 azure/scope/machinepool.go:928-930); AKS replica write-back exists as `SetCAPIMachinePoolReplicas` (managedmachinepool.go:328-334). Critically, on the fork's own spectro-master the const has ZERO consumers and the two ManagedMachinePoolScope helpers have ZERO callers (verified by grep) — the reconciler that used them was replaced when the fork rebased to v1.18.
- **resolution guidance:** Recommend NEEDS-DECISION → SKIP. Carrying it would resurrect dead code plus a non-standard annotation string that nothing (CAPZ or CAPI) reads on the target. This resolves the #35-vs-#42 asymmetry: #35's diff is empty (auto-SKIP), #42's diff survives textually (hence the const.go conflict) but is behaviorally obsolete. Verify Palette does not set/read `cluster.x-k8s.io/replicas-managed-by-autoscaler` anywhere (if it does, map it to the standard `replicas-managed-by` annotation in Palette, not in CAPZ).

# 7b180e1d — AKS static placement for cross RG
- **cluster:** aks-managed-clusters · **plan decision (r2):** NEEDS-DECISION
- **tickets:** none
- **PR:** none — direct push (Snehal Amrutkar, 2022-09-08)
- **behavior:** Static placement: let Palette place AKS clusters onto a pre-existing VNet/subnet in a DIFFERENT resource group than the cluster RG (`virtualNetwork.resourceGroup` on AMCP). Rebased remnant is manifest-only: +252 lines AMCP CRD yaml, +589 lines spectro/generated (core-base/core-global), all Go type/conversion files show 0-line (empty) changes.
- **pr/ticket context:** no ticket/PR context; behavior inferred from diff only.
- **parity vs v1.26.0:** UPSTREAMED (behavior) + REGEN (payload). v1.26.0 `ManagedControlPlaneVirtualNetwork` has `ResourceGroup string` (`api/v1beta1/azuremanagedcontrolplane_types.go:381-384`) — cross-RG BYO VNet is first-class upstream. CRD/spectro yaml regenerate.
- **resolution guidance:** SKIP / REGEN. One behavior nuance to preserve elsewhere: upstream's webhook makes `virtualNetwork.resourceGroup` immutable on update; the fork relaxes that when the OLD value is empty (legacy v1.3.2 clusters) — that relaxation lives in the fork's AMCP webhook (see 0cb06ceb note / cluster summary) and must be re-applied in `internal/webhooks/azuremanagedcontrolplane_webhook.go` (`validateVirtualNetworkUpdate`), NOT via this commit.

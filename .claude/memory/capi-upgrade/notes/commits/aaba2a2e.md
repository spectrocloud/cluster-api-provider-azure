# aaba2a2e — Manifest Fixes II
- **cluster:** cloud-environments (previous-upgrade reconcile subgroup) · **plan decision (r2):** NEEDS-DECISION (MIXED; Regression Watch Low)
- **tickets:** none
- **PR:** none — direct push (2025-03-12, v1.18 rebase repair)
- **behavior:** Small manifest corrections after 79c0d269: webhook patch/certmanager cert name+namespace alignment (`config/capz/manager_webhook_patch.yaml`, `config/default/manager_webhook_patch.yaml`, `config/certmanager/certificate.yaml`), one line in `config/manager/manager.yaml`, and `spectro/generated/core-{base,global}.yaml` touch-ups. Supports the fork's webhook/controller pod-split deployment (the `:9443` split owned by the 248e1eb1/5e819cea cluster).
- **pr/ticket context:** none
- **parity vs v1.26.0:** **SUPERSEDED (REGEN/vendorcrd).** `spectro/generated/*` is regenerated by palette vendorcrd-sync at integration; `config/*` webhook patches are re-derived when the pod-split commits are resolved against v1.26 kustomize layout.
- **resolution guidance:** SKIP as a standalone pick. The behavior it protects (webhook/controller split wiring) must be verified in the vendorcrd-sync output (`vendorcrd-sync verify --provider azure`), not by this commit's SHA presence.

# efd40404 — PCP-1467: PR Comments Resolution I
- **cluster:** aks-managed-clusters · **plan decision (r2):** NEEDS-DECISION (conflict on azure/services/managedclusters/spec.go)
- **tickets:** PCP-1467 (same as e2975f92)
- **PR:** review-comment follow-up to the PCP-1467 change (pushed to spectro-master)
- **behavior:** Cleanup of e2975f92: removed the duplicated identity-forcing block from the update path and instead normalized `Identity.UserAssignedIdentities` (keys only, values emptied) on both sides of `computeDiffOfNormalizedClusters`, so Azure-added identity metadata (clientId/principalId) doesn't cause a perpetual diff/PUT loop.
- **pr/ticket context:** commit message says "PR Comments Resolution"; no surviving review thread.
- **parity vs v1.26.0:** SUPERSEDED-as-dead. Entire computeDiff apparatus is commented out / stubbed at 0164d80e; ASO owns drift comparison at v1.26.0. No live behavior to preserve beyond what e2975f92's note covers.
- **resolution guidance:** SKIP with the PCP-1467 unit (aece2202 + e2975f92 + efd40404). Take upstream at any spec.go conflict.

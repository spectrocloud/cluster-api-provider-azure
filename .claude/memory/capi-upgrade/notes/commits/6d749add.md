# 6d749add — Bypassing webhook validation for Azure SharedGallery
- **cluster:** webhook-runtime-infra · **plan decision (r2):** PICK (heuristic "applies cleanly, fork-only")
- **tickets:** none referenced
- **PR:** none — direct push (Vishwanath Taykhande, Apr 2025)
- **behavior:** Intermediate step of the gallery-validation relaxation: comments out the `image.SharedGallery != nil` branch of `validateSingleDetailsOnly` in `api/v1beta1/azureimage_validation.go`, so an Image carrying BOTH SharedGallery and ComputeGallery no longer gets rejected. Reason (in-diff comment): during the palette CAPZ v1.18 upgrade, palette populates both galleries to avoid node repave when moving between gallery types. Fully rewritten 3 weeks later by 45d9bb84 into a proper either/or (ComputeGallery preferred) instead of a comment-out.
- **pr/ticket context:** none; behavior inferred from diff comments ("Commenting this out since after CAPZ upgrade to v1.18.0, palette will be using both ComputeGallery & Shared Gallery … not cause node repave").
- **parity vs v1.26.0:** **SUPERSEDED by fork commit 45d9bb84** (same file, rewrites this hunk). Not upstreamed — v1.26.0 `internal/webhooks/azureimage_validation.go::validateSingleDetailsOnly` still enforces strict mutual exclusivity.
- **resolution guidance:** Treat 6d749add + 45d9bb84 as one unit whose NET diff is 45d9bb84's final shape. If both are picked in sequence the intermediate state is harmless, but both land on the DEAD path `api/v1beta1/azureimage_validation.go` (file removed at v1.26.0 — moved to internal/webhooks), so the behavior would be silently lost either way — see 45d9bb84 note for the mandatory re-target. Cleanest: SKIP 6d749add, carry the net change via 45d9bb84 re-targeted to internal/webhooks.

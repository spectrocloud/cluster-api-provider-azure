# 32e8a1f1 — Decouple the env checking and mapping
- **cluster:** cloud-environments (#118) · **plan decision (r2):** NEEDS-DECISION (conflicts: cluster.go, machinepool.go, managedcontrolplane.go, util/azure/azure.go+test)
- **tickets:** none in JIRA batch
- **PR:** #118
- **behavior:** Implements the reviewer's fix to e8b25112: mapping now happens ONLY when (1) the scope's cloud is `AzureUSSecretCloud` AND (2) the resource-manager endpoint contains `.scombine.scloud` (Sequoia emulator suffix). Real AzSecret (or any cloud with a genuine `ussec` region) keeps the original location. Adds tests.
- **pr/ticket context:** author: "Use ENV simulator check first… if we don't, there's a case that ussec is the real AZC and we still map even if that is online."
- **parity vs v1.26.0:** **FORK-ONLY must-carry.** This guarded form is exactly what survives at S in all three scopes.
- **resolution guidance:** Land with the location-mapping unit (see e8b25112). Note machinepool.go upstream churned heavily F..T (±182) — re-anchor the `Location()` wrapper and ensure `ScaleSetSpec()` uses `m.Location()` (not `m.AzureMachinePool.Spec.Location`) on the new base; that one-line call-site change is part of the contract.

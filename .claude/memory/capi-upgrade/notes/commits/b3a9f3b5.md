# b3a9f3b5 — Recover cluster setting with ENV checking, and apply TLS version fix
- **cluster:** cloud-environments (#121) · **plan decision (r2):** NEEDS-DECISION (conflicts: azure_secret_cloud.go, cluster.go) — **highest-value conflict in the cluster**
- **tickets:** PEM-5484 (branch name; no JIRA batch context)
- **PR:** #121 "Pem 5484 add env checking"
- **behavior:** Repairs the **public-cloud regression** the AzureSecret work caused (PR #120 context: Azure public + private LB broke). Splits `cluster.go` into two dispatched code paths: `publicIPSpecsForStandard/lbSpecsForStandard/apiServerHostForStandard/setDNSNameForStandard` = byte-restored upstream behavior for all normal clouds; `…ForAzureSecret` variants = nil-safe versions (nil APIServerLB/FrontendIPs guards in `APIServerPublicIP()`/`APIServerPrivateIP()`, tolerant outbound-LB public-IP spec building) for AzSecret. Every public entry point (`PublicIPSpecs`, `LBSpecs`, `APIServerLBName`, `APIServerLBPoolName`, `APIServerHost`, `SetDNSName`) dispatches on `isAzureSecretCloudEnvironment()` FIRST. Also sets `MinVersion: tls.VersionTLS12` on the AzSecret global transport (go-vuln fix).
- **pr/ticket context:** PR body: "apply fully two sets of code for different ENV, to make the key logic before and after #117 separate… added minTLS version in azsecret.go which resolves one of the go Vuls." cPu1 review: use `isAzureSecretCloudEnvironment()` helper (done in 2d63d1f9).
- **parity vs v1.26.0:** **FORK-ONLY must-carry — but the Standard copies are STALE upstream snapshots.** ⚠ This is the cluster's #1 regression trap: the `…ForStandard` bodies are frozen copies of the upstream implementation as of ~v1.18/19 (they already include APIServerILB logic). Upstream changed `cluster.go` by ±196 lines F..T; carrying the old copies verbatim would silently revert v1.19→v1.26 LB/publicIP fixes **for every Azure cluster, not just AzSecret**.
- **resolution guidance:** Preserve the dispatch pattern + the ForAzureSecret variants, but **regenerate every `…ForStandard` body from v1.26.0's actual `PublicIPSpecs`/`LBSpecs`/`APIServerHost`/`SetDNSName` implementations** (copy target code into the Standard functions, or better: have the dispatcher fall through to the upstream implementation renamed). Reviewer must diff each ForStandard body against v1.26.0's original — any divergence is a regression. TLS12 MinVersion is already inside S-tip azure_secret_cloud.go. Regression Watch entry: HIGH (public-cloud blast radius).

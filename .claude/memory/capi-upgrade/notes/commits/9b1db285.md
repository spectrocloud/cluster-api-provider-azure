# 9b1db285 — Removal of comments
- **cluster:** cloud-environments (previous-upgrade reconcile subgroup) · **plan decision (r2):** NEEDS-DECISION (conflict: internal/api/v1beta1/azurecluster_default.go)
- **tickets:** none
- **PR:** none — direct push (2025-04-22)
- **behavior:** Deletes 6 `fmt.Println` debug lines ("These are the current subnets", "node subnet name...") from `api/v1beta1/azurecluster_default.go` that an earlier subnet-feature replay commit left in. Pure debug-noise cleanup; no behavior change.
- **pr/ticket context:** none
- **parity vs v1.26.0:** **SUPERSEDED.** v1.26.0 never had these prints; the file moved to `internal/api/v1beta1/azurecluster_default.go` (hence the conflict).
- **resolution guidance:** SKIP. Only relevant if the subnet-default cascade is replayed with the debug prints intact — instead resolve that cascade to its net S-tip form (which already has no prints).

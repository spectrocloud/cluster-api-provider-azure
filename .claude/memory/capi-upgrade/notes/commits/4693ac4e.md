# 4693ac4e — Change the Fix to service level
- **cluster:** networking · **plan decision (r2):** PICK-VERIFY
- **tickets:** PCP-5171 (via PR title)
- **PR:** #125 (see c42079f9)
- **behavior:** Final form of PR #125 and the chain's NET payload. In `azureMachineService.delete` (controllers/azuremachine_reconciler.go): if the `virtualmachine` service's Delete returns a transient 5xx (`azure.IsTransientServerError`: azcore.ResponseError 500/502/503/504), run `verifyVMDeletionWithBoundedRetry` — up to 10 rounds × 5 s, each a single `virtualmachines.NewClient(...).Get` with 15 s timeout; if the VM is confirmed gone (ResourceNotFound), CONTINUE deleting the remaining services instead of failing the AzureMachine delete. Any ambiguity fails safe (treat as still-existing → surface the original error). Purpose: Azure US Secret Cloud (and flaky ARM) returns 500 even when VM deletion succeeded; without this, machines wedge in Deleting.
- **pr/ticket context:** empty PR body; title carries PCP-5171. Removes 4f838de6's controller-level attempt.
- **parity vs v1.26.0:** FORK-ONLY must-carry. v1.26.0 `azureMachineService.delete` is the plain reverse-order loop (no 5xx tolerance); `virtualmachines.NewClient(auth, apiCallTimeout)` signature matches, so the NET applies with minimal friction.
- **resolution guidance:** Apply chain as unit / squash to NET (errors.go + azuremachine_reconciler.go only; async.go + azuremachine_controller.go net-zero). Keep the check keyed on service Name()=="virtualmachine" and the fail-safe defaults. Regression-watch note: the bounded retry can block a reconcile worker up to ~50 s + 10×15 s Gets during genuinely failed deletes — acceptable per fork history, but worth a CBT delete-path check. Verify `IsTransientServerError` doesn't collide with target-side errors.go additions.

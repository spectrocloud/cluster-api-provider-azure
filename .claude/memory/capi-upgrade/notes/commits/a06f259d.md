# a06f259d — AKS UpgradeChannels (#92)
- **cluster:** aks-managed-clusters · **plan decision (r2):** NEEDS-DECISION (mixed generated+code)
- **tickets:** PCP-1614 (from PR title "PCP-1614: AKS UpgradeChannels")
- **PR:** #92 "PCP-1614: AKS UpgradeChannels" (empty body)
- **behavior:** Adds AKS auto-upgrade support: `spec.autoUpgradeProfile.upgradeChannel` (none/patch/stable/rapid/node-image) on the AMCP → ManagedCluster AutoUpgradeProfile, so Palette users can enable AKS-managed Kubernetes auto-upgrades. Includes scope pass-through, computeDiff normalization (SDK era), tests, go.mod bumps, and large spectro/generated + CRD regen.
- **pr/ticket context:** PR body empty; behavior from diff + ticket title.
- **parity vs v1.26.0:** UPSTREAMED — by the same author. Upstream commit `631a4b27c5` "support for auto upgrade channel" (LochanRn, 2023-10-13) landed the equivalent one month after this fork commit; v1.26.0 has `UpgradeChannel` type + constants (types :43-72), `Spec.AutoUpgradeProfile` (scope :675-679), `ManagedClusterAutoUpgradeProfile` in the service spec (spec.go :141-157) and `configureAutoUpgradeProfile` (spec.go :444).
- **resolution guidance:** SKIP; take upstream (field names/json identical since it's the same author's design). go.mod/go.sum hunks are superseded; yaml is REGEN. The companion downgrade-protection is d8e1ce40 (also upstreamed) — see that note.

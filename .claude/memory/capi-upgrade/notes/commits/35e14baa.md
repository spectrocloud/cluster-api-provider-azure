# 35e14baa — PCP-328 fix
- **cluster:** aks-managed-clusters · **plan decision (r2):** SKIP (empty on cumulative tree)
- **tickets:** PCP-328 — "Continuously seeing 'successfully reconciled' in AKS cluster" (Bug, Closed): after AKS scale-up, event spam continued for hours; fix = stop redundant reconcile work/events in the managedclusters loop.
- **PR:** none — direct push (Jayesh Srivastava, authored 2022-09-13)
- **behavior:** Contract: no perpetual no-op update/event churn on a steady-state AKS cluster — CAPZ should only act (and emit) when desired != actual.
- **pr/ticket context:** ticket only; the commit is an EMPTY commit in the rebased spectro-master history (git diff-tree: no tree change, single parent) — its content was absorbed during a prior rebase into the cumulative computeDiff-based no-op suppression (`computeDiffOfNormalizedClusters` → "diff == \"\" → return nil, nil"), which itself is now commented out at 0164d80e.
- **parity vs v1.26.0:** UPSTREAMED (architecturally) — **SKIP verified correct.** The SDK-era pattern (unconditional PUT each loop → "successfully reconciled" spam) no longer exists: v1.26.0 reconciles the ManagedCluster through ASO, which computes drift server-side and only patches/PUTs on actual change. The failure mode PCP-328 describes cannot occur in the ASO architecture, and the fork itself has been running without the client-side diff since the v1.18 rebase.
- **resolution guidance:** SKIP safe — empty commit, behavior superseded by ASO. Nothing for the resolver to preserve. If reconcile-noise regressions appear in CBT, look at ASO/controller event configuration, not this commit.

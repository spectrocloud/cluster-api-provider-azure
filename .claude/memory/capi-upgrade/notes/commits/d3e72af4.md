# d3e72af4 — PCP-6137 Handle CAPZ vulenerability
- **cluster:** cve-build-noise · **plan decision (r2):** SKIP
- **tickets:** PCP-6137 — same ticket as `79f5be32` (see batch3: "No description. Pure CVE-bump work on the fork (go.mod/go.sum). Commits already SKIPped").
- **PR:** #139 "PCP-6137 Handle CAPZ vulenerability" (merged 2026-03-05T09:45:09Z) — same PR record as `79f5be32`; this commit lands ~2 minutes after it (author timestamp 15:08:12 vs 15:06:23 same day), i.e. a follow-up commit within the same PR/ticket rather than an independent PR.
- **behavior:** Further `go.mod`/`go.sum` bumps continuing PCP-6137's cleanup: `go.opentelemetry.io/otel/sdk` and `.../otel/sdk/metric` 1.35.0→1.40.0; `github.com/cloudflare/circl` (indirect) 1.6.1→1.6.3; `golang.org/x/sys` (indirect) 0.32.0→0.40.0. Same contract as `79f5be32` — complete the OTel/crypto/sys dependency bump for the flagged CVE(s).
- **pr/ticket context:** none beyond what's captured under `79f5be32`'s PR #139 — no distinguishing comments.
- **parity vs v1.26.0:** SUPERSEDED — same reasoning as `79f5be32`: target floor (go 1.25.0→1.26.4 later in this cluster; OTel/circl/sys all pinned much newer) fully subsumes the CVEs this commit closed.
- **resolution guidance:** Confirmed touches only `go.mod`/`go.sum`. Safe to SKIP for the same reason as `79f5be32` — treat the pair (`79f5be32`, `d3e72af4`) as one logical PCP-6137 change-set; both are superseded together, no partial-carry scenario applies.

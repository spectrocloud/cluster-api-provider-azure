# aece2202 — adding UserAssignedIdentities to AzureManagedControlPlane
- **cluster:** aks-managed-clusters · **plan decision (r2):** NEEDS-DECISION
- **tickets:** none on the commit; lineage of PCP-1467
- **PR:** none — direct push (Luther Monson, 2023-06-16)
- **behavior:** Adds `spec.userAssignedIdentities []UserAssignedIdentity{providerID}` to the AMCP class spec (`api/v1beta1/types_class.go:252+`) so Palette's AKS pack can hand a user-assigned managed identity to the cluster; scope + managedclusters spec plumb it into the ManagedCluster identity. This is the Palette-facing API field that PCP-1467 later made actually work.
- **pr/ticket context:** no ticket/PR context; behavior inferred from diff + PCP-1467 lineage.
- **parity vs v1.26.0:** FORK-ONLY (API surface) but **functionally DEAD on the current fork**: at 0164d80e the scope populates `managedClusterSpec.UserAssignedIdentities` (managedcontrolplane.go:716-722) and even builds a discarded local slice (":778-785 — dead code"), but `spec.go` Parameters NEVER consumes `s.UserAssignedIdentities`; the ASO identity is driven solely by upstream's `spec.identity` (`getIdentity`, spec.go:622-626/869-893). Upstream v1.26.0 has no `userAssignedIdentities` field — its equivalent is `Identity{Type: UserAssigned, UserAssignedIdentityResourceID}` (types L587-601).
- **resolution guidance:** Decision input for Gate 1: the behavior was silently lost in a previous rebase (v1.18/ASO migration) — Palette AKS UAI today must be flowing via `spec.identity` or ASO patches, else it is broken in production already. Recommend: (a) confirm with Palette whether it still sets `userAssignedIdentities`; (b) if yes, either wire it to `s.Identity`/ASO or migrate Palette to upstream `identity.userAssignedIdentityResourceID`; (c) if no, drop the field + dead scope code instead of carrying it. Do NOT blind-carry — it re-adds dead API surface.

# 1c814218 — update removal flow
- **cluster:** aks-managed-clusters · **plan decision (r2):** NEEDS-DECISION (conflict on azure/services/managedclusters/spec.go)
- **tickets:** none
- **PR:** none — direct push (LochanRn, 2023-07-05)
- **behavior:** SDK-era addon REMOVAL semantics: AKS's PUT API leaves addons untouched when omitted, so when an addon is removed from the AMCP spec the fork explicitly wrote it back as `Enabled: false` (preserving Config), and stripped `Identity` from existing addon profiles to avoid diff noise. Contract: removing an addon from the Palette pack actually disables it in AKS.
- **pr/ticket context:** no ticket/PR context; behavior inferred from diff only.
- **parity vs v1.26.0:** SUPERSEDED-as-dead. The hunk lives only inside the commented-out block at 0164d80e (`spec.go:776-795`) — the disable-on-removal behavior has been DEAD on the fork since the v1.18/ASO rebase. Upstream v1.26.0 `configureAddonProfiles` (spec.go:642-658) builds addonProfiles purely from spec and does NOT write Enabled:false for removed addons.
- **resolution guidance:** SKIP for this upgrade (no regression vs current fork behavior — it's equally absent today). BUT record in Regression Watch: "addon removal may not disable the addon in AKS" is a latent product gap on both current fork and v1.26.0; if Palette QA (CBT pack-ops scenario) expects removal→disable, it needs a NEW fix (likely via `asoManagedClustersPatches` or an upstream PR), not a cherry-pick of this dead commit.

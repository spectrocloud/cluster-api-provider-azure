# 9972a7a1 — Suppress warning messages for ServicePrincipal auth
- **cluster:** aks-managed-clusters · **plan decision (r2):** NEEDS-DECISION (conflict on controllers/azurejson_{machine,machinepool,machinetemplate}_controller.go)
- **tickets:** none
- **PR:** none — direct push (Snehal Amrutkar, 2022-08-29)
- **behavior:** Comments out the `spIdentityWarning` log line + `VMIdentityNone` Warning event in the three azure-json controllers. Palette provisions with Service Principal credentials and `identity: None` on every machine, so unsuppressed this fires a Warning event per machine per reconcile — event/log spam at fleet scale. Contract: no recurring "Service Principal authentication is less secure" warning events on Palette-managed clusters.
- **pr/ticket context:** no ticket/PR context; behavior inferred from diff only.
- **parity vs v1.26.0:** FORK-ONLY must-carry (if the silence is still wanted). v1.26.0 STILL emits the warning, now gated by `isUsingSPCredentials(...)` (azurejson_machine_controller.go:240-242, machinepool:179-183, machinetemplate:198-200). Since Palette DOES use SP credentials, the upstream gate does not silence it — dropping the fork hunk re-introduces the spam.
- **resolution guidance:** Trivial 3-file carry: inside each upstream `if ... VMIdentityNone [&& isUsingSPCredentials]` block, comment out / delete the `log.Info` + `r.Recorder.Eventf` lines (keep the surrounding gate so future upstream churn stays mergeable). Risk: none functional — purely observability. Alternative: accept upstream behavior and drop the hunk if event noise is now tolerable; that is a product call, flag at Gate 1.

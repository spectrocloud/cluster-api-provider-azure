# f1abc73a — fix(converters): forward OsSKU to embedded ManagedCluster agent pool profile
- **cluster:** aks-managed-clusters · **plan decision (r2):** NEEDS-DECISION (conflict on azure/converters/managedagentpool.go)
- **tickets:** PCP-6938 (⚠️ HIGH-VALUE, SUS-1748 / SCS-4830 / RCA-SCS-4830): AKS system pool with osSKU=AzureLinux born as Ubuntu, then unnecessarily repaved (or never fixed when CP gated).
- **PR:** #157 "PCP-6938: forward OsSKU to embedded ManagedCluster agent pool profile" (pavansokkenagaraj, merged 2026-07-01; detailed body)
- **behavior:** `AgentPoolToManagedClusterAgentPoolProfile` copied a fixed field list that omitted `OsSKU`; Palette injects osSKU via `asoManagedClustersAgentPoolPatches`, applied before the system pool is embedded into `ManagedCluster.Spec.AgentPoolProfiles` at create — so the dropped field meant AKS defaulted the system pool to Ubuntu. Fix: one line `OsSKU: properties.OsSKU` in the converter. Contract: the system (first) node pool is born with the user's osSKU (e.g. AzureLinux), no intermediate Ubuntu + repave. Plus fork-only characterization test `azure/converters/sus1748verify/verify_test.go` (isolated package because in-package converter tests are pre-existing-broken on spectro-master — they import ASO v1api20231001 while prod moved to v1api20240901).
- **pr/ticket context:** PR body documents root cause, nil-safety (`*string` copy, omitempty), backport to spectro-release-4.9 (customer on 4.9.16), and the broken in-package tests being out of scope.
- **parity vs v1.26.0:** **UPSTREAMED** (evidence): v1.26.0 `azure/converters/managedagentpool.go` includes `OsSKU: properties.OsSKU` in the profile literal — added by upstream commit `6080b8a197` "Ensure all agent pool parameters are preserved in managed cluster" (Jon Huhn, 2025-03-27), which rewrote the converter to copy EVERY field of the hub type (now `v1api20250801/storage`) and added a NOTE + linter expectation that the field list stays complete. PCP-6938's exact bug is structurally prevented upstream.
- **resolution guidance:** SKIP the converter line — at conflict, take upstream's full field list (superset). Decide on the test: `sus1748verify/verify_test.go` is fork-only and pinned to v1api20240901 types; either drop it (upstream's converter tests + linter cover the invariant) or port it to the v1.26.0 hub version (`v1api20250801/storage`) as a cheap characterization guard. T2 post-condition: converter output for a pool with `OsSKU: AzureLinux` carries OsSKU (upstream test already asserts field completeness).

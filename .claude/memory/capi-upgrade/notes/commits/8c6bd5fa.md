# 8c6bd5fa — Fix Azure Private Cluster
- **cluster:** networking · **plan decision (r2):** NEEDS-DECISION (conflicts: types_class.go, azure/scope/cluster.go, loadbalancers/spec.go, internal/api/v1beta1/azurecluster_default.go, internal/webhooks/azurecluster_validation.go)
- **tickets:** none on commit (private-cluster family; no ticket/PR context — behavior inferred from diff)
- **PR:** none — direct push
- **behavior:** Restores the `PrivateIP` field that 3d6a0310 over-removed and hardens the feature: (1) re-adds `LoadBalancerClassSpec.PrivateIP` + defaulting (use it over 10.0.0.100) + LBSpec plumbing; (2) fixes nil-deref in validateAPIServerLB (`old != nil` guard on the Static-immutability check — create path passed old=nil); (3) in `getFrontendIPConfigs` (loadbalancers/spec.go) only sends `PrivateIPAddress` to Azure when `IPAllocationMethod == "Static"` — for Dynamic, an empty address lets Azure assign one (which the Reconcile read-back then persists).
- **pr/ticket context:** none.
- **parity vs v1.26.0:** FORK-ONLY must-carry (with 601023f0 chain). v1.26.0 has neither field; its `getFrontendIPConfigs` unconditionally sets `PrivateIPAddress: ptr.To(ipConfig.PrivateIPAddress)` with `PrivateIPAllocationMethod: Static`; its validateAPIServerLB (internal/webhooks) enforces UNCONDITIONAL private-IP immutability for internal LBs — incompatible with the fork's dynamic flow.
- **resolution guidance:** Part of the LB unit NET. spec.go: keep the `if lbSpec.IPAllocationMethod == "Static"` gate around `privateIPAddress`, preserve upstream's new zone-redundancy (`Zones`) handling in the same function (f8729a7feb). Validation: port the fork's conditioned immutability into internal/webhooks/azurecluster_validation.go REPLACING upstream's unconditional `old.PrivateIPAddress != lb.PrivateIPAddress → Forbidden` for internal LBs — otherwise the ""→assigned-IP update from the read-back is rejected and private clusters wedge. Also drop/reconcile upstream's `privateIPCount != 1` internal check against dynamic mode (frontend has empty PrivateIPAddress until first reconcile completes). Defaulting: port into internal/api/v1beta1 `setDefaultAzureClusterAPIServerLB`.

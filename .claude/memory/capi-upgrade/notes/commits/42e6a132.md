# 42e6a132 — Updated the check for updating machinepool when using autoscalar (#35)
- **cluster:** networking · **plan decision (r2):** SKIP (empty on cumulative tree — duplicate)
- **tickets:** none
- **PR:** fork PR #35 (no context file captured; pr_refs only)
- **behavior:** EMPTY commit in the linearized fork history (0 files — verified `git show --name-only`). Original intent (July 2022, same author as #42): don't fight an external cluster-autoscaler over MachinePool replica counts — skip/align replica updates when the autoscaler manages node count. Content vanished in an earlier fork rebase because upstream absorbed the behavior.
- **pr/ticket context:** none captured.
- **parity vs v1.26.0:** UPSTREAMED (as behavior): v1.26.0 handles externally-managed replicas via CAPI's standard annotation — `annotations.ReplicasManagedByExternalAutoscaler` in `MachinePoolScope.HasReplicasExternallyManaged` (azure/scope/machinepool.go:928-930) + `ReconcileReplicas`, and `SetCAPIMachinePoolReplicas` for AKS pools (azure/scope/managedmachinepool.go:328).
- **resolution guidance:** SKIP correct and safe — empty diff. This also answers the #35-vs-#42 question: #35 is empty (nothing to conflict), while its re-fix #42 (aef95617) still has a nonzero residual diff that conflicts on azure/const.go, hence the differing engine decisions. See aef95617.

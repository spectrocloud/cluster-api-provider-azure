# 363e2980 — Location mapping support for AzureSecret
- **cluster:** cloud-environments (#122) · **plan decision (r2):** PICK-VERIFY (Regression Watch Med)
- **tickets:** PEM-5484 (branch: PEM-5484-location-mapping-support)
- **PR:** #122 "Location mapping support for AzureSecret" — "A copy of #119. When merging #121 it was marked as merged, while not actually."
- **behavior:** Re-lands the guarded location mapping removed by e5e74b7c: `util/azure` gains `NormalizeAzureRegion` + `regionMapping{ussec→eastus2}` (+ `ConvertResourceGroupNameToLower` context) with tests; `MachinePoolScope.Location()` (new method, used by `ScaleSetSpec`) and `ManagedControlPlaneScope.Location()` map the region only when cloud==AzureUSSecretCloud AND endpoint contains `.scombine.scloud`. Completes emulator-aware region aliasing for VMSS + AKS scopes (cluster.go part came via #121).
- **pr/ticket context:** see #118 review thread (guard requirement) and #121/#122 merge mixup.
- **parity vs v1.26.0:** **FORK-ONLY must-carry.** Exact surviving form at S for machinepool.go/managedcontrolplane.go/util-azure.
- **resolution guidance:** PICK-VERIFY is right; the VERIFY must confirm (a) no duplicate mapping code from e8b25112 landed (pairs with the e5e74b7c SKIP), (b) `ScaleSetSpec` on v1.26 machinepool.go calls `m.Location()` — upstream restructured that file (±182 F..T), so re-anchor the call site, (c) util/azure/azure.go merges cleanly with upstream's own F..T additions (+40 upstream).

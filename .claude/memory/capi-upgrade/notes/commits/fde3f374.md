# fde3f374 — Changed init cert acquire method
- **cluster:** cloud-environments (#117 chain) · **plan decision (r2):** NEEDS-DECISION (conflicts: azure_secret_cloud.go, identity.go)
- **tickets:** PEM-7511
- **PR:** #117
- **behavior:** Moves certificate acquisition to startup/init (cert read once from the environment folder alongside env JSONs) instead of inside each credential creation in `identity.go` (-82 there); main.go loses the explicit call (-7). Establishes "cert handled at initialization time, credentials just consume the global transport" — the comment still present in S-tip identity.go.
- **pr/ticket context:** guyni review: "don't need a fallback certPath — if no environment folder is set, don't load any CA cert, like current public clouds" (enforced fully by c1a28ceb).
- **parity vs v1.26.0:** **FORK-ONLY must-carry (net form).** v1.26.0 identity.go has no transport injection; the fork's per-credential `ConfigureAzIdentityOptions` calls + init-time cert load must be re-applied.
- **resolution guidance:** Part of #117 net diff. Final S state: cert comes from ConfigMap `azure-capz-cert-config` (key `azure-ca.crt`) via `initializeGlobalTransportWithCertData`; identity.go calls `ConfigureAzIdentityOptions(&options.ClientOptions)` per credential type. Re-apply onto v1.26.0's identity.go noting upstream now has Cloud config on the client-secret path already (keep upstream's, add fork extras: DisableInstanceDiscovery, transport, Cloud config on cert/MSI paths).

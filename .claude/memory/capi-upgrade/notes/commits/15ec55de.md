# 15ec55de — PCP-1595 and PCP-1596 fix (#83)
- **cluster:** aks-managed-clusters · **plan decision (r2):** NEEDS-DECISION (mixed generated+code)
- **tickets:** PCP-1595 — private DNS zone input for AKS from Palette AMCP; PCP-1596 — DNSPrefix support for AKS.
- **PR:** #83 "PCP-1595 and PCP-1596 fix" (body is template boilerplate)
- **behavior:** Palette-facing AKS inputs: (a) `spec.dnsPrefix` (defaulting to the AMCP name when unset) → ManagedCluster DnsPrefix; (b) `spec.fqdnSubdomain` for private clusters with a CUSTOM private DNS zone (Azure requires fqdnSubdomain instead of dnsPrefix in that mode); (c) `spec.dockerBridgeCidr`; (d) `apiServerAccessProfile.privateDNSZone` pass-through + computeDiff normalization. Together these enable Palette's private-AKS-with-custom-DNS-zone story.
- **pr/ticket context:** PCP-1595 "input from palette managedcontrolplane" (analogous to self-managed PEM-483/484); PCP-1596 adds DNSPrefix that upstream then lacked.
- **parity vs v1.26.0:** PARTIAL.
  - DNSPrefix: UPSTREAMED (`api/v1beta1/azuremanagedcontrolplane_types.go:167-170`, scope :563, spec.go :497; webhook defaults it to cluster name AND makes it immutable on update — `internal/webhooks/azuremanagedcontrolplane_webhook.go:121-127`).
  - PrivateDNSZone: UPSTREAMED (`APIServerAccessProfile.PrivateDNSZone`, scope :633, spec.go :681; plus `GetPrivateDNSZoneName` helper).
  - **FqdnSubdomain: FORK-ONLY must-carry.** v1.26.0 has NO AMCP `fqdnSubdomain` field or scope/spec plumbing (only ASO-level support reachable via `asoManagedClusterPatches`; grep of api/, scope/, spec.go is empty). Fork carries field (types :176-179 area), scope pass-through, and `managedCluster.Spec.FqdnSubdomain` set (spec.go :523 @0164d80e).
  - DockerBridgeCidr: FORK-ONLY-DEAD — declared + scope-passed (@0164d80e scope :640) but never consumed in spec.go; AKS retired dockerBridgeCidr and ASO v1api20240901+ has no such field. Safe to drop.
- **resolution guidance:** Carry ONLY the FqdnSubdomain slice (AMCP class-spec field + scope pass + `managedCluster.Spec.FqdnSubdomain = s.FqdnSubdomain` in Parameters + CRD regen) — or migrate Palette to `asoManagedClusterPatches`. Drop DockerBridgeCidr. Watch two upstream semantic additions: DNSPrefix immutability and its default-to-cluster-name webhook (fork defaulted in scope; results identical at create, but pre-existing CRs without dnsPrefix will get it stamped by the webhook on next update — benign but visible in diffs). Regression Watch: private-cluster-with-custom-DNS-zone creation path (CBT create scenario on Azure) exercises fqdnSubdomain.

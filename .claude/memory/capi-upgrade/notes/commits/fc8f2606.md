# fc8f2606 — Support for Azure Private DNS Zone to be present in any resource group
- **cluster:** networking · **plan decision (r2):** SKIP (already in T)
- **tickets:** none on commit (private-DNS feature family, PEM-483/484 adjacent)
- **PR:** #114 "Support for Azure Private DNS Zone to be present in any resource group" (merged 2025-05-05, base spectro-v1.18.0, author vishu2498; body is the untouched upstream PR template — no extra signal)
- **behavior:** Adds `NetworkSpec.PrivateDNSZoneResourceGroup` so a user-supplied private DNS zone can live in a different resource group than the cluster. Wired into `PrivateDNSSpec()` (zone/link/record specs use the override RG), plus webhook validation (only valid together with `PrivateDNSZoneName`, RG-name regex, immutable on update) and CRD schemas.
- **pr/ticket context:** PR body is template-only. Same author (Vishwanath Taykhande) later contributed the identical change upstream.
- **parity vs v1.26.0:** UPSTREAMED — upstream commit 6e62f2cdd0 (Mar 2025, same author/title) is in 1957fc65..v1.26.0. Evidence: v1.26.0 api/v1beta1/types_class.go:462-465 (`PrivateDNSZoneResourceGroup`), azure/scope/cluster.go:583-584 (RG override in PrivateDNSSpec), validation now in internal/webhooks/azurecluster_validation.go (post webhook-move fbb3b95524).
- **resolution guidance:** SKIP is correct and safe — target is patch-equivalent (Spectro upstreamed its own change). Verify post-execute that `privateDNSZoneResourceGroup` is present in the regenerated CRDs and that the fork's private-DNS chain (e176e7db NET) composes with the RG override in `PrivateDNSSpec()` — both touch the same block of azure/scope/cluster.go.

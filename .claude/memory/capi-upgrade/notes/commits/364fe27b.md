# 364fe27b — dockerfile: fixed duplicate builds (#147) (#149)
- **cluster:** webhook-runtime-infra · **plan decision (r2):** NEEDS-DECISION (conflict on Dockerfile)
- **tickets:** none
- **PR:** #149 "dockerfile: fixed duplicate builds (#147)" (kpiyush17, merged 2026-05-19; backport of #147, no body/comments)
- **behavior:** Removes the redundant first `go build` invocation 1a8918cf's Dockerfile had left in (the binary was compiled twice — once without `-o manager`, once with), roughly halving image build time. No runtime behavior change; keeps the FIPS/static build fork + assert/govulncheck steps intact.
- **pr/ticket context:** title only; behavior obvious from diff.
- **parity vs v1.26.0:** **FORK-ONLY must-carry, but only as part of the fork Dockerfile.** Upstream's Dockerfile never had the duplicate step; this fix is meaningless outside 1a8918cf's file.
- **resolution guidance:** Fold into the 1a8918cf resolution: take the fork Dockerfile at S-tip (which equals 1a8918cf ∘ 364fe27b ∘ later builder-image tweaks). If commits are replayed individually, apply 364fe27b directly after 1a8918cf so the intermediate double-build state never needs its own conflict resolution. No verify needed beyond T1 image build (Patch-H stage will exercise it).

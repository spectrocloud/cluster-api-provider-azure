# 8b5ab295 — enforce lowercase providerID RG to match cloud-provider-azure
- **cluster:** networking · **plan decision (r2):** PICK-VERIFY
- **tickets:** none (upstream cherry-pick, author Jack Francis / upstream maintainer)
- **PR:** none — direct push (fork-side)
- **behavior:** cloud-provider-azure lowercases the resource-group segment of node providerIDs; CAPZ must produce matching providerIDs or Machine↔Node matching breaks (nodes never get providerID-matched, machines stuck). Adds `ConvertResourceGroupNameToLower` (util/azure) and applies it when setting the VMSS providerID in scalesets.Reconcile, including a deferred read-back of VMSS state.
- **pr/ticket context:** none fork-side; this is upstream's own fix (upstream commit family 0f321e4089/5c7f372477/9a95035d5d — Aug 2022) cherry-picked into the fork's pre-v1.18 history.
- **parity vs v1.26.0:** UPSTREAMED — answer to the special-attention question: YES, v1.26.0 normalizes providerID casing itself, now via the cloud-provider-azure library (`azprovider.ConvertResourceGroupNameToLower`) at azure/services/scalesets/scalesets.go:147, azure/services/virtualmachines/virtualmachines.go:116, azure/converters/vmss.go:115, controllers/azuremanagedmachinepool_reconciler.go:161. The v1.18 fork base already contained it too.
- **resolution guidance:** Recommend downgrading PICK-VERIFY → SKIP. Re-applying would re-introduce the local util/azure copy of the helper (duplicate of the azprovider one) and an outdated Reconcile structure over upstream's reworked scalesets flow. Post-execute verify: `git grep ConvertResourceGroupNameToLower` on the result shows only azprovider call sites, and VMSS providerIDs in a CBT run are lowercase-RG.

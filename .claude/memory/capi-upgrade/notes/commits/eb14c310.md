# eb14c310 — Applied Vishu's Comments
- **cluster:** cloud-environments (#117 chain) · **plan decision (r2):** NEEDS-DECISION (conflicts across const.go, azure_secret_cloud.go, cluster.go, managedclusters.go, link_reconciler.go, both controllers)
- **tickets:** PEM-7511
- **PR:** #117
- **behavior:** Review-fix sweep: adds `DisablePrivateDNSAnnotation` (`capz.io/disable-private-dns`) as a named constant in `azure/const.go` (+6) and uses it in cluster.go instead of a string literal; converts remaining debug prints to leveled `log.V(1)` in managedclusters.go/controllers; replaces a hand-rolled min func with `math.Min`; documents the link_reconciler dup-link logic.
- **pr/ticket context:** implements vishu2498's inline comments verbatim (constant for annotation key, `math.Min`, keep the privatedns debug log, log levels).
- **parity vs v1.26.0:** **FORK-ONLY (folded into net).** All surviving forms are in the S-tip files.
- **resolution guidance:** Subsumed by the #117 net diff. The `azure/const.go` +6 (DisablePrivateDNSAnnotation + ReplicasManagedByAutoscalerAnnotation doc block) applies cleanly to v1.26 const.go.

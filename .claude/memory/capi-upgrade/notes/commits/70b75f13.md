# 70b75f13 — PCP-1567: AKS ServiceCIDR Fix (#81)
- **cluster:** aks-managed-clusters · **plan decision (r2):** SKIP (empty on cumulative tree)
- **tickets:** PCP-1567 — GMF customer escalation: AKS create failed `DnsServiceIpOutOfServiceCidr` because only DNSServiceIP was sent while ServiceCidr fell back to Azure's default 10.0.0.0/16. (Commit body also references PCP-1467 — mislabel; content is PCP-1567.)
- **PR:** #81 "PCP-1567: AKS ServiceCIDR Fix" (body is template boilerplate, no extra signal)
- **behavior:** Contract: when a custom serviceCIDR and/or dnsServiceIP are set on the AKS profile, BOTH `NetworkProfile.ServiceCidr` and `NetworkProfile.DnsServiceIP` must be sent to Azure — never DNSServiceIP alone.
- **pr/ticket context:** PCP-1567 root cause (Lochan Rn): the else-branch in `azure/services/managedclusters/spec.go` set only DNSServiceIP, missing `managedCluster.NetworkProfile.ServiceCidr = &s.ServiceCIDR`. QA verified fixed.
- **parity vs v1.26.0:** UPSTREAMED — **SKIP verified correct.** (1) The commit is literally an EMPTY commit in the rebased spectro-master history (git diff-tree shows no changes; content was absorbed into the cumulative spec.go state). (2) The live fork block (`spec.go:528-541` @0164d80e) and v1.26.0's `configureServiceCIDR` (`spec.go:545-561`: `managedCluster.Spec.NetworkProfile.ServiceCidr = &s.ServiceCIDR; ... DnsServiceIP = s.DNSServiceIP; if s.DNSServiceIP == nil { calculateDNSServiceIP() }`) are semantically identical: whenever ServiceCIDR is set, ServiceCidr is ALWAYS sent, alongside the custom or derived DNS IP. PCP-1567's exact failure mode cannot recur.
- **resolution guidance:** SKIP safe — no content to apply and target covers the contract. Suggest a T2 post-condition: ASO ManagedCluster rendered from an AMCP with custom serviceCIDR+dnsServiceIP contains both NetworkProfile fields (upstream `spec_test.go` already covers this path).

# 72906f59 — Revert "Revert "PEM-483: palette changes to input privatednszone""
- **cluster:** networking · **plan decision (r2):** PICK-VERIFY
- **tickets:** PEM-483 (private-DNS FQDN scheme apiserver.<clustername>.<customdns>)
- **PR:** none — direct push
- **behavior:** Re-applies a8ff143f verbatim (revert of the revert; message references sha e174277e — a pre-rebase copy of adb3f75a). Restores 2-arg `GeneratePrivateFQDN`, `getHostName()`, and the `PrivateDNSSpec`/`APIServerHost` call sites. Still unconditionally applies the new scheme; the conditional fix lands next in e176e7db.
- **pr/ticket context:** none; behavior inferred from diff + chain chronology.
- **parity vs v1.26.0:** N/A alone — part of the revert chain; NET state = a8ff143f content + e176e7db conditioning. v1.26.0 has no equivalent (see a8ff143f note).
- **resolution guidance:** Same as adb3f75a: replay the whole chain or squash to NET. The `getHostName()` helper introduced here survives verbatim into the fork's current spectro-master and must land next to `PrivateDNSSpec()` in v1.26.0's azure/scope/cluster.go (~line 607), inside the new `PrivateDNSZoneMode() != None` guard.

# 5fb2ad0a — Use same subnet for cp and workers
- **cluster:** networking · **plan decision (r2):** NEEDS-DECISION (MIXED generated+code)
- **tickets:** none (no ticket/PR context; behavior inferred from diff only)
- **PR:** none — direct push
- **behavior:** Introduces fork-only subnet role `all` (`SubnetAll`): a single subnet serves both control-plane and worker nodes. Defaulting treats role=all as a node subnet; validation stops requiring separate node/control-plane roles when an `all` subnet exists (and skips duplicate-name accounting for it); `GetControlPlaneSubnet`/`UpdateControlPlaneSubnet` match role=all; `MachineScope.SetSubnetName` places machines into the all-subnet without requiring explicit subnetName. Also excludes NAT-gateway-enabled subnets from the IPv6 outbound-LB requirement. Palette cluster profiles emit `role: all`, so existing AzureCluster objects carry it — dropping the enum value breaks CRD validation on upgrade.
- **pr/ticket context:** none. Note original commit has an apparent bug (`cpSubnet.SecurityGroup...setDefaults` referenced inside the node loop) — verify the fork's CURRENT net state, not this literal diff.
- **parity vs v1.26.0:** FORK-ONLY must-carry. v1.26.0 has no `all` role (only `cluster` role, which is upstream's own combined-subnet concept — functionally similar but a different API string, so not a substitute for existing Palette clusters). Target also reworked SetSubnetName with a clusterSubnetName fallback (azure/scope/machine.go:742) and moved defaulting/validation to internal/api + internal/webhooks.
- **resolution guidance:** Split per engine advice: regen the generated parts (CRD yamls, spectro/generated) and hand-port the code part. Code targets moved: subnet defaulting → internal/api/v1beta1/azurecluster_default.go `setDefaultAzureClusterSubnets`/`setDefaultSubnetSpec*`; validation → internal/webhooks/azurecluster_validation.go `validateSubnets`; keep api/v1beta1/types.go `SubnetAll` + enum `;all` in types_class.go. In SetSubnetName, merge fork's `subnetAllSpecified` bypass WITH upstream's new clusterSubnetName fallback (treat role=all like role=cluster there). Long-term consideration (not this upgrade): migrate Palette to upstream's `cluster` role to shrink the fork.

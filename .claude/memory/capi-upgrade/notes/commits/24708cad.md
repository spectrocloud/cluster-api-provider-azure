# 24708cad — PCP-6412: added enableAzureRBAC flag for aadProfile (#142)
- **cluster:** aks-managed-clusters · **plan decision (r2):** NEEDS-DECISION (mixed generated+code)
- **tickets:** PCP-6412 — AKS AAD feature forces Azure RBAC; PWC needs AAD/Entra authN with **Kubernetes RBAC** authZ. QA validated both paths from console.
- **PR:** #142 "PCP-6412: added enableAzureRBAC flag for aadProfile" (kpiyush17, merged 2026-04-09; empty body/comments)
- **behavior:** Adds `aadProfile.enableAzureRBAC bool` (optional, default false) to the AMCP API and passes it through the scope instead of upstream's hardcoding. Contract: an AAD-managed AKS cluster is created/kept with Azure RBAC ON or OFF per the user's flag — enabling AAD must NOT silently force Azure RBAC authorization.
- **pr/ticket context:** PCP-6412: upstream enables Azure RBAC unconditionally when AADProfile is set and exposes no disable knob; fork flag lets clusters be created/upgraded with either K8s RBAC or Azure RBAC.
- **parity vs v1.26.0:** **FORK-ONLY must-carry — HIGH regression risk.** Verified: v1.26.0 `AADProfile` has ONLY `Managed` + `AdminGroupObjectIDs` (types :297-306, no EnableAzureRBAC field), and the scope hardcodes `EnableAzureRBAC: s.ControlPlane.Spec.AADProfile.Managed` (`azure/scope/managedcontrolplane.go:595`). The downstream service spec/ASO plumbing (`managedclusters.AADProfile.EnableAzureRBAC`, spec.go :180-181, :621) exists upstream — only the API field + scope wiring are fork-specific. If dropped, every AAD cluster with `enableAzureRBAC: false` silently flips to Azure RBAC on the next reconcile (authZ-mode regression, PWC escalation redux).
- **resolution guidance:** Carry as a unit: (1) `api/v1beta1/azuremanagedcontrolplane_types.go` — add `EnableAzureRBAC bool \`json:"enableAzureRBAC,omitempty"\`` to AADProfile; (2) `azure/scope/managedcontrolplane.go:595` — replace `AADProfile.Managed` with `AADProfile.EnableAzureRBAC`; (3) regen CRDs (azuremanagedcontrolplanes + azuremanagedcontrolplanetemplates) — the commit's yaml hunks are REGEN. Watch: upstream may add AADProfile webhook validation (`internal/webhooks/azuremanagedcontrolplane_*`) — ensure nothing rejects the extra field semantics. Add to Regression Watch as High; include a T2 post-condition: AMCP with `aadProfile.managed: true, enableAzureRBAC: false` renders ASO `AadProfile.EnableAzureRBAC=false`.

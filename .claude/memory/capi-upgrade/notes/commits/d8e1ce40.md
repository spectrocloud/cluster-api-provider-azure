# d8e1ce40 — ensure to not downgrade an auto-upgraded cluster (#95)
- **cluster:** aks-managed-clusters · **plan decision (r2):** NEEDS-DECISION (conflict on 8 files incl. scope, spec.go, mocks, go.mod)
- **tickets:** none (companion to PCP-1614 auto-upgrade work)
- **PR:** #95 "ensure to not downgrade an auto-upgraded cluster" (body is template boilerplate)
- **behavior:** When `autoUpgradeProfile.upgradeChannel` is patch/stable/rapid, AKS may raise the cluster's Kubernetes version above the user's declared `spec.version`. CAPZ must then honour the higher running version instead of "downgrading" back to spec (AKS rejects downgrades). Mechanism: `GetManagedClusterVersion` picks `max(spec.Version, existing.KubernetesVersion)` via `versions.GetHigherK8sVersion`; scope gains `IsManagedVersionUpgrade`/`Is{Patch,Stable,Rapid}AutoUpgrade` + `SetAutoUpgradeVersionStatus` recording the auto-upgraded version in `Status.AutoUpgradeVersion`.
- **pr/ticket context:** PR body boilerplate; behavior from diff.
- **parity vs v1.26.0:** UPSTREAMED. v1.26.0 has `Status.AutoUpgradeVersion` (types :410-413), `SetAutoUpgradeVersionStatus` + channel checks (scope :524-538), post-reconcile capture `if scope.IsManagedVersionUpgrade() { scope.SetAutoUpgradeVersionStatus(currentKubernetesVersion) }` (managedclusters.go :125-130), and the same max-version guard in agent pools (`agentpools/spec.go:193 versions.GetHigherK8sVersion`) and machine-pool scope (managedmachinepool.go :362-366). Same design (LochanRn upstreamed the fork work).
- **resolution guidance:** SKIP; take upstream everywhere (scope, spec.go, mocks, tests, go.mod are all superseded). T2 post-condition idea: with upgradeChannel=stable and existing version > spec version, rendered ASO ManagedCluster keeps the higher version.

# 16ca18d3 — AKS: enable isVnetManaged, add caching
- **cluster:** networking · **plan decision (r2):** PICK-VERIFY
- **tickets:** none (upstream cherry-pick, author Jack Francis)
- **PR:** none — direct push (fork-side)
- **behavior:** Upstream fix (family 0f321e4089): make `IsVnetManaged` actually consult the VNet's owned-tag for AKS and cache the result on the scope, so BYO-VNet clusters aren't treated as managed. In the fork's linearized history the recorded diff has shrunk to ONE residual line: `FrontendIPConfigs: s.APIServerLB().FrontendIPs` in `ClusterScope.LBSpecs()` — a rebase artifact; the isVnetManaged/caching content itself is already in the tree.
- **pr/ticket context:** none.
- **parity vs v1.26.0:** UPSTREAMED/SUPERSEDED — answer to the special-attention question: the caching IsVnetManaged is upstream code and present in BOTH the v1.18 fork base and v1.26.0 (azure/scope/cluster.go:670-688, `s.cache.isVnetManaged`, now ASO-based Get + `Tags.HasOwned`); the fork's "caching hook" needs no carry. The residual LBSpecs line is SUPERSEDED: v1.26.0 populates `FrontendIPConfigs` itself with smarter selection (loop picking the public IP, or the single internal IP, for the APIServerILB feature) instead of passing all FrontendIPs.
- **resolution guidance:** Recommend downgrading PICK-VERIFY → SKIP. If picked "cleanly", the struct-literal line would be silently overridden by upstream's subsequent selection loop (harmless but dead); worst case it fights the APIServerILB dual-frontend logic. Nothing of fork value is lost by skipping — fork validation restricts the API-server LB to 1 frontend IP anyway, making upstream's selection equivalent.

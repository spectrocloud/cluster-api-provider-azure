# a8ff143f — PEM-484: palette changes to input privatednszone
- **cluster:** networking · **plan decision (r2):** PICK-VERIFY
- **tickets:** PEM-484 (palette passes custom privateDNSZone input to CAPZ); sibling PEM-483 (FQDN scheme apiserver.<clustername>.<customdns>)
- **PR:** none — direct push
- **behavior:** When the user supplies a custom `NetworkSpec.PrivateDNSZoneName`, the private API-server FQDN and the DNS A-record hostname become `apiserver.<clusterName>.<zone>` instead of upstream's `apiserver.<zone>`. This lets multiple clusters share one custom private DNS zone without record collisions (the default generated zone already embeds the cluster name; a custom zone does not). Changes `GeneratePrivateFQDN` (azure/defaults.go) to a 2-arg form and adds `ClusterScope.getHostName()` used by `PrivateDNSSpec()` record hostname + `APIServerHost()`.
- **pr/ticket context:** PEM-483 comment links fork PR #56; PEM-484 has no description. Note: commit message says PEM-484 but the revert pair references PEM-483 — same change, both sub-tasks of the private-DNS feature.
- **parity vs v1.26.0:** FORK-ONLY must-carry. v1.26.0 `azure/defaults.go:138` still has 1-arg `GeneratePrivateFQDN` returning `apiserver.<zone>`, and `PrivateDNSSpec` (azure/scope/cluster.go:620) still hardcodes `Hostname: azure.PrivateAPIServerHostname`. No upstream equivalent.
- **resolution guidance:** This commit alone is superseded WITHIN the fork chain by e176e7db (3-arg form). Apply the chain as a unit or take only the NET state (see e176e7db note). Target functions still exist at azure/defaults.go:138 and azure/scope/cluster.go:620/1003; upstream's "Remove dead code" (782f0f346f) did NOT touch them. New in target: `PrivateDNSSpec` is now guarded by `PrivateDNSZoneMode() != None` (upstream 59c26c982f) — apply the Hostname change inside that guard. Callers of GeneratePrivateFQDN must all be updated to the 3-arg form (compile-checked).

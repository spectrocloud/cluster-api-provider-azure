# 0fc4e9d8 — updated computediff authorized ip range logic for nil check
- **cluster:** aks-managed-clusters · **plan decision (r2):** NEEDS-DECISION (conflict on azure/services/managedclusters/spec.go)
- **tickets:** none
- **PR:** none — direct push (LochanRn, 2022-09-24)
- **behavior:** SDK-era normalization: when the AMCP spec no longer carries `apiServerAccessProfile.authorizedIPRanges`, explicitly set the outgoing ManagedCluster's `AuthorizedIPRanges = nil` so (a) removed IP ranges are actually cleared in Azure and (b) the old `computeDiffOfNormalizedClusters` didn't see a spurious nil-vs-empty diff every loop.
- **pr/ticket context:** no ticket/PR context; behavior inferred from diff only.
- **parity vs v1.26.0:** SUPERSEDED by the ASO architecture. On the fork today the surviving hunk (`spec.go:611-615` @0164d80e) is an `else if` that sets nil on a freshly-constructed ASO object — a no-op. v1.26.0 `configureAPIServerAccessProfile` (spec.go:685-687) builds the ASO ManagedCluster fresh each reconcile and only sets AuthorizedIPRanges when non-nil; ASO applies the whole spec, so absent = cleared. The original diff-suppression problem no longer exists (no client-side computeDiff).
- **resolution guidance:** SKIP-safe / drop at conflict. If the 3-way resolver hits this region of spec.go, take upstream's `configureAPIServerAccessProfile` as-is. No behavioral loss.

# 55d361d7 — adding OutboundType to AzureManagedControlPlane
- **cluster:** aks-managed-clusters · **plan decision (r2):** NEEDS-DECISION
- **tickets:** none
- **PR:** none — direct push (Luther Monson, 2023-06-16)
- **behavior:** Expose AKS `outboundType` (loadBalancer / managedNATGateway / userAssignedNATGateway / userDefinedRouting) on the AMCP so Palette can create egress-restricted / NAT-gateway AKS clusters; spec.go sends it in NetworkProfile.
- **pr/ticket context:** no ticket/PR context; behavior inferred from diff only.
- **parity vs v1.26.0:** UPSTREAMED. v1.26.0 has `ManagedControlPlaneOutboundType` + `Spec.OutboundType` (`api/v1beta1/azuremanagedcontrolplane_types.go:75-86,140`), scope pass-through (`azure/scope/managedcontrolplane.go:559`), and NetworkProfile wiring (`azure/services/managedclusters/spec.go:87-88,537-538`). Same json name `outboundType`.
- **resolution guidance:** SKIP; take upstream. Semantic difference to note: v1.26.0's webhook makes `outboundType` IMMUTABLE on update (`internal/webhooks/azuremanagedcontrolplane_webhook.go:140-145`, citing the AKS preview limitation) — the fork never enforced this. If any Palette flow mutates outboundType post-create it will now be rejected by admission; expected to be a non-issue (Azure itself doesn't support changing it GA), but worth a CBT eye.

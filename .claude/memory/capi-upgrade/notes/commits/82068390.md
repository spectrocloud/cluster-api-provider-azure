# 82068390 — disableLocal accounts (#91)
- **cluster:** aks-managed-clusters · **plan decision (r2):** NEEDS-DECISION (mixed generated+code)
- **tickets:** PCP-1615 (from PR title "PCP-1615: disableLocal accounts")
- **PR:** #91 "PCP-1615: disableLocal accounts" (empty body)
- **behavior:** AKS security hardening for AAD clusters: (a) `spec.disableLocalAccounts` pass-through to the ManagedCluster; (b) since disabling local accounts removes admin-credential access, the kubeconfig flow changes — fetch the USER credentials, then mint a non-interactive kubeconfig by exchanging AAD credentials for a bearer token (`azure/services/token/client.go`) and stripping the `kubelogin` exec block (`auth.Token = token; auth.Exec = nil`), so Palette (which has no kubelogin) can still talk to the cluster. Adds scope helpers `IsAadEnabled`/`IsLocalAcountsDisabled`, admin/user kubeconfig getters/setters, `ReconcileKubeconfig`.
- **pr/ticket context:** PR body empty; behavior from diff.
- **parity vs v1.26.0:** UPSTREAMED. v1.26.0 carries the whole design: `Spec.DisableLocalAccounts` (types :221-223, scope :598-599, spec.go :625-630), `AreLocalAccountsDisabled`/`IsAADEnabled` (scope :501-517 — renamed, typo fixed), and `reconcileKubeconfig` + `getUserKubeconfigData` + `getUserKubeConfigWithToken` in `azure/services/managedclusters/managedclusters.go:135-160` (token logic folded into the service; no separate `azure/services/token` package upstream, it uses azidentity inline).
- **resolution guidance:** SKIP; take upstream. During execute, ensure the fork's `azure/services/token/` package and mock updates aren't half-carried by neighboring picks (dead duplicate). Behavior watch at T3/CBT: AAD + disableLocalAccounts cluster kubeconfig must remain token-based (no exec/kubelogin) — upstream does exactly this, including token refresh on each reconcile ("Always fetch credentials in case of rotation", managedclusters.go:110).

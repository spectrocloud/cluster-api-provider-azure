# e3c6f868 — Fix for webhook validation of AzureManagedControlPlane VNET Resource Groups
- **cluster:** webhook-runtime-infra · **plan decision (r2):** PICK (heuristic "applies cleanly, fork-only")
- **tickets:** none referenced
- **PR:** none — direct push (Vishwanath Taykhande, Apr 2025)
- **behavior:** Relaxes the AMCP update-immutability check on `spec.virtualNetwork.resourceGroup`: old palette AKS clusters created with CAPZ v1.3.2 stored an EMPTY VNET ResourceGroup; CAPZ ≥v1.18 defaults it to a real value, so on the first update after upgrade the strict `old != new` check rejected the object and **blocked cluster upgrades**. Fork check: `old.Spec.VirtualNetwork.ResourceGroup != "" && old != new` — i.e. empty→set transitions are allowed once, real mutations still rejected.
- **pr/ticket context:** none; intent is spelled out in the in-diff comment ("allows the old clusters to upgrade").
- **parity vs v1.26.0:** **FORK-ONLY must-carry — but the plan's PICK is a trap.** The commit edits `api/v1beta1/azuremanagedcontrolplane_webhook.go`, which does NOT exist at v1.26.0; the live check moved to `internal/webhooks/azuremanagedcontrolplane_validation.go` → `validateAzureManagedControlPlaneVirtualNetworkUpdate()` (~line 533), which at v1.26.0 is still the STRICT `old != new` form. A "clean" pick recreates the old file as dead code in package api/v1beta1 while the live webhook keeps rejecting empty→set — silently reintroducing the upgrade-blocker for every pre-v1.18-era AKS cluster.
- **resolution guidance:** Re-target: apply the empty-string guard to `internal/webhooks/azuremanagedcontrolplane_validation.go` (`if old.Spec.VirtualNetwork.ResourceGroup != "" && old.Spec.VirtualNetwork.ResourceGroup != m.Spec.VirtualNetwork.ResourceGroup`). Do NOT let the pick create `api/v1beta1/azuremanagedcontrolplane_webhook.go`. Add to Regression Watch (it is currently absent) and cover with a unit test: old RG empty + new RG set → no error; old RG set + changed → error. Recommend flipping decision PICK → PICK-RESOLVE/NEEDS-DECISION with explicit re-target instruction.

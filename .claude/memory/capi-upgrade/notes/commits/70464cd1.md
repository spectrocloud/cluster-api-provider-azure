# 70464cd1 — Cherrypicked upstream azure env changes for managed cluster aks on gov cloud (#105)
- **cluster:** cloud-environments · **plan decision (r2):** NEEDS-DECISION (MIXED; Regression Watch HIGH)
- **tickets:** PCP-1159 — CAPZ changes so palette can set a gov-cloud parameter on AKS (AzureManagedControlPlane); links upstream issue kubernetes-sigs#3489
- **PR:** #105 (spectro fork; original author Shubham Rajvanshi, 2024-01-26, replayed onto v1.18 base 2025-02-21)
- **behavior:** Lets an AKS (managed) cluster target a non-public Azure cloud by adding `AzureEnvironment` to AzureManagedControlPlaneSpec + defaulting/webhook support, so credentials and ARM endpoints resolve for AzureUSGovernmentCloud.
- **pr/ticket context:** PCP-1159 explicitly says this cherry-picks the upstream fix for kubernetes-sigs#3489.
- **parity vs v1.26.0:** **UPSTREAMED.** Mirrors upstream commit 571b77166e ("add AzureEnvironment to AzureManagedControlPlaneSpec", Luther Monson), merged via upstream PR kubernetes-sigs/cluster-api-provider-azure#3509, shipped in v1.10.0+ — present in v1.26.0 (`api/v1beta1/types_class.go` AzureManagedControlPlaneClassSpec.AzureEnvironment; webhook immutability check in `internal/webhooks/azuremanagedcontrolplane_webhook.go:111`; `ARMClientOptions` USGovernmentCloud switch in `azure/defaults.go`). It was ALSO already in the fork's own base v1.18.0 (fork-point ce2dbec7) — so the replayed commit's real content is: (a) resurrected stale `exp/api/v1beta1/azuremanagedcontrolplane_{types,webhook}.go` (1033 lines that no longer belong on v1.18+, deleted again by aa405349), (b) gofmt/import-order noise in main.go, (c) a CRD yaml hunk the target already has.
- **resolution guidance:** Recommend **flip to SKIP** (with aa405349's corresponding delete also skipped — they cancel). Nothing to preserve: the gov-cloud contract is native in v1.26.0. Downgrade the Regression Watch HIGH: verify by grepping target for `AzureEnvironment` in AMCP spec + `USGovernmentCloudName` in ARMClientOptions (both present). If picked anyway, it only re-creates dead exp/ files that break `make generate` drift checks.

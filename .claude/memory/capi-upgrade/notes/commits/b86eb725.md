# b86eb725 — Further removed unnecessary stuff
- **cluster:** cloud-environments (#117 chain) · **plan decision (r2):** NEEDS-DECISION (conflicts: azure_secret_cloud.go, managedclusters.go, azureasomanagedcontrolplane_controller.go)
- **tickets:** PEM-7511
- **PR:** #117
- **behavior:** Consolidates kubeconfig CA injection: adds `getCustomCACertificateForAMCP` + the CA-append flow to `azuremanagedcontrolplane_reconciler.go` (+95 — non-ASO AKS path), extends the ASO controller version (+62), trims raw debug prints from `managedclusters.go` (-51 net), small azure_secret_cloud + main.go adjustments. After this, BOTH AKS reconcile paths (classic AMCP and ASO-managed) patch the AzSecret CA chain into generated kubeconfigs so palette/agents can trust the private AKS endpoint.
- **pr/ticket context:** snehala27/vishu2498: use proper leveled loggers (finished in eb14c310/d13d31d0).
- **parity vs v1.26.0:** **FORK-ONLY must-carry.** Neither controller in v1.26.0 does CA injection into kubeconfig secrets.
- **resolution guidance:** Preserve via the #117 net: `controllers/azuremanagedcontrolplane_reconciler.go` (+102 net) and `controllers/azureasomanagedcontrolplane_controller.go` (+81 net) apply nearly clean on v1.26 (upstream churn there is minor). The injected cert source is `azure.AzSecretCertData` — requires the defaults.go bridge vars to land first.

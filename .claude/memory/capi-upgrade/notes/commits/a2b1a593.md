# a2b1a593 — fix for add-on profile upgrade
- **cluster:** aks-managed-clusters · **plan decision (r2):** NEEDS-DECISION (conflict on azure/services/managedclusters/spec.go)
- **tickets:** none
- **PR:** none — direct push (LochanRn, 2023-07-04)
- **behavior:** SDK-era update-path hardening in `Parameters()`: (a) transient-error (retry in 20s) when the existing ManagedCluster is in a non-terminal provisioning state; (b) LoadBalancerProfile normalization so AKS-added EffectiveOutboundIPs don't produce spurious diffs; (c) never mutate agent pools via AMCP (reuse existing profiles — pools are AMMP-owned); (d) `computeDiffOfNormalizedClusters` — skip the Azure PUT entirely when the normalized desired == existing (this is also the PCP-328 "reconcile spam" remedy).
- **pr/ticket context:** no ticket/PR context; behavior inferred from diff only.
- **parity vs v1.26.0:** SUPERSEDED / dead on the fork. At 0164d80e the whole block is COMMENTED OUT (`spec.go:762-816`) and `computeDiffOfNormalizedClusters` survives only as a trivial ported stub (`spec.go:944`, unused). The ASO architecture supersedes each piece: ASO owns PUT/PATCH semantics and drift detection (no client-side diff), agent-pool embedding is handled by `AgentPoolToManagedClusterAgentPoolProfile` + preserve-existing logic upstream (see upstream 6080b8a197), provisioning-state races are ASO's concern.
- **resolution guidance:** SKIP; at any spec.go conflict take upstream v1.26.0. Do not resurrect the commented block or the stub function — recommend deleting the stub during execute (build-green loop) rather than carrying it. No net behavior change vs today's fork (already dead).

# 93449d5c — fix for BET-3719
- **cluster:** webhook-runtime-infra · **plan decision (r2):** PICK (heuristic "applies cleanly, fork-only")
- **tickets:** BET-3719 (not in provided JIRA batches; original intent recovered from pre-replay history)
- **PR:** none — direct push (Snehal Amrutkar, Jul 2022, replayed)
- **behavior:** In the replayed history this commit's ENTIRE content is the creation of a **0-byte file** `exp/api/v1beta1/azuremanagedmachinepool_webhook.go`. The original BET-3719 fix (pre-replay commits e7c68a19a7 / b7a0d1bb9e) DELETED the "last system node pool cannot be mutated/deleted" validation from the AMMP webhook so palette could manage AKS node pools freely. That behavior did **not** survive: at S (0164d80e) `validateLastSystemNodePool` exists and is enforced in `api/v1beta1/azuremanagedmachinepool_webhook.go` (lines 218/303) with logic identical to upstream (annotation-based skip supported). The empty file itself was later removed at S (path absent).
- **pr/ticket context:** no ticket/PR context in inputs; original diff recovered from branch history (e7c68a19a7 "added fix for BET-3719 (#26)").
- **parity vs v1.26.0:** **SUPERSEDED / abandoned.** S and v1.26.0 enforce the same last-system-node-pool validation (v1.26.0: `internal/webhooks/azuremanagedmachinepool_validation.go:41`, byte-equivalent modulo package qualifiers). There is no fork behavior left to preserve.
- **resolution guidance:** **Change PICK → SKIP.** Picking is actively harmful: it creates an empty `.go` file in package `exp/api/v1beta1` (which exists at v1.26.0) → `expected 'package', found EOF` compile error at Tier-1 smoke, unless a later NEEDS-DECISION commit (aa405349 "Removing extra exp files and code") happens to delete it again. Zero value, real build risk. If any reviewer wants BET-3719's original relaxation back, that is a NEW product decision, not a carry.

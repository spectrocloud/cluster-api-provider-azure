# f918f3cf — added new support for AzureSecret Region name and cfg map
- **cluster:** cloud-environments (#117 chain, PIVOT commit) · **plan decision (r2):** NEEDS-DECISION (conflicts: defaults.go, azure_secret_cloud.go)
- **tickets:** PEM-7511 (delivers the PEM-7501 design: JSON → ConfigMap → mounted → loaded)
- **PR:** #117
- **behavior:** THE architectural pivot of the feature: replaces env-var/folder file loading with **ConfigMap-driven initialization** — constants `azure-capz-env-config` / `azure-capz-cert-config` (keys `azure-capz-env.json` / `azure-ca.crt`), new `InitializeAzureConfigForCluster(ctx, client, namespace, env)` reading both ConfigMaps from the cluster namespace, `processAzureEnvironmentJSON` (JSON → temp file → `azure.EnvironmentFromFile` → `SetEnvironment`), cert → `initializeGlobalTransportWithCertData`. Also renames the cloud to its real name **`AzureUSSecretCloud`** (`AzSecretCloudName` in defaults.go).
- **pr/ticket context:** answers snehala27's "what happens where AZURE_ENVIRONMENT_FOLDER was used — this no more exists now": deliberately retired.
- **parity vs v1.26.0:** **FORK-ONLY must-carry.** Core of the surviving contract; nothing comparable upstream.
- **resolution guidance:** The definitive content is S-tip `azure_secret_cloud.go` (plus the `InitializeAzureConfigForCluster` call at the top of `NewClusterScope` in cluster.go — non-fatal on error, logs and continues with defaults). Replaying intermediates before this commit and then this one re-creates the whole file churn; prefer jumping straight to S-tip state.

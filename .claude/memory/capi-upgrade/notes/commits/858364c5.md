# 858364c5 — Fixed upgrade issue from v1alpha4 to v1beta1
- **cluster:** aks-managed-clusters · **plan decision (r2):** NEEDS-DECISION (conflict on spectro/generated/core-base.yaml, core-global.yaml)
- **tickets:** none
- **PR:** none — direct push (Snehal Amrutkar, 2022-09-22)
- **behavior:** Fix for old (CAPZ v1.3.2-era) Palette AKS clusters upgrading into the v1beta1 world: default `agentPoolSpec.OSType = Linux` when the AMMP doesn't set it (`azure/scope/managedmachinepool.go` buildAgentPoolSpec), so conversion/reconcile of legacy pools doesn't send an empty osType to Azure. Plus large spectro/generated yaml refresh.
- **pr/ticket context:** no ticket/PR context; behavior inferred from diff only.
- **parity vs v1.26.0:** UPSTREAMED (code) + REGEN (yaml). The scope-level default is still live on the fork (`azure/scope/managedmachinepool.go:206-208` @0164d80e), but v1.26.0 defaults OSType in the AMMP mutating webhook (`internal/webhooks/azuremanagedmachinepool_webhook.go:66-67` → `ptr.To(infrav1.DefaultOSType)`), which covers every object admitted through the API server — a superset of the fork's scope default for CR-driven flows.
- **resolution guidance:** SKIP the code hunk (upstream webhook default supersedes it); the spectro/generated conflict is REGEN territory (vendorcrd-sync/kustomize regenerates). One residual to verify at T2: objects that bypass the mutating webhook (e.g. direct controller-runtime writes in tests) would rely on the scope default — acceptable; AKS itself also defaults osType=Linux server-side.

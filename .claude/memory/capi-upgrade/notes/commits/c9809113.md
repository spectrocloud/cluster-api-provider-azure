# c9809113 — fix: reuse global TracerProvider
- **cluster:** cve-build-noise · **plan decision (r2):** SKIP
- **tickets:** none directly; PR body cites upstream cherry-pick (this is NOT a Spectro-authored fix or a dependency bump — flagged separately from the rest of the cluster per the shared-agent brief's instruction to verify it independently).
- **PR:** #116 "fix: reuse global TracerProvider" (merged 2025-06-09T06:57:07Z, base `spectro-v1.18.0`). Body: **"Upstream cherry-pick https://github.com/kubernetes-sigs/cluster-api-provider-azure/pull/5540"**.
- **behavior:** Touches 4 `.go` files (NOT go.mod/go.sum/build files — this is the one commit in the cluster with real code changes): `azure/defaults.go`, `azure/scope/identity.go`, `controllers/aso_credential_cache.go`, `pkg/ot/traces.go`. Replaces three call sites that were independently constructing a *new* OTLP `TracerProvider` per Azure ARM client (`ot.OTLPTracerProvider(ctx)`, which does network I/O and can error) with a single call to `otel.GetTracerProvider()`, the process-wide global provider registered once at controller startup. Also unexports the now purely-internal `OTLPTracerProvider` → `otlpTracerProvider` in `pkg/ot/traces.go`. Contract: avoid constructing a redundant tracer provider (and the error path that comes with it) per client-options call — reuse the one global provider already registered for the whole manager process. Purely an internal wiring/correctness fix; no user-visible behavior change.
- **pr/ticket context:** PR body is explicit and unambiguous that this is a straight cherry-pick of upstream kubernetes-sigs/cluster-api-provider-azure#5540, not fork-original work. No review comments recorded.
- **parity vs v1.26.0:** **UPSTREAMED (verified from diff, not just the PR claim)**. Traced the original upstream commit: `git log --all --grep="reuse global TracerProvider"` finds `f4efea5f7ebfcbdefee0636f001c9fa86ca6bb02` "fix: reuse global TracerProvider" (Michael Weibel, 2025-04-02, from PR kubernetes-sigs/cluster-api-provider-azure#5540, merged via `b18718cf79`, and cherry-picked upstream itself onto release-1.18/1.19 via `#5544`/`#5545`). Confirmed `f4efea5f7e` is an ancestor of `v1.26.0` (`git merge-base --is-ancestor f4efea5f7e v1.26.0` → true). Directly diffed the target tree against this commit's changes: `v1.26.0`'s `azure/defaults.go:344`, `azure/scope/identity.go:94`, and `controllers/aso_credential_cache.go:102` all read `azotel.NewTracingProvider(otel.GetTracerProvider(), nil)` — byte-for-byte the same pattern this fork commit introduces. `pkg/ot/traces.go` still exists upstream too (RegisterTracing helper retained for the manager-startup registration path).
- **resolution guidance:** No conflict risk — this fork commit and the target tree converge on identical code at the three call sites. SKIP is correct and does not lose any fork-specific behavior (there is none to lose; this was never fork-original, it was cherry-picked FROM upstream in the first place and upstream's own v1.26.0 line already has it natively). Nothing for a 3-way resolver to reconcile.

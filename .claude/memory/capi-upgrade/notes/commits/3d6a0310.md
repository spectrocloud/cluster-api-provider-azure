# 3d6a0310 — cleanup
- **cluster:** networking · **plan decision (r2):** NEEDS-DECISION (conflicts: types_class.go, azure/scope/cluster.go, loadbalancers/{loadbalancers,spec}.go, internal/api/v1beta1/azurecluster_default.go)
- **tickets:** none (no ticket/PR context; behavior inferred from diff only)
- **PR:** none — direct push
- **behavior:** Feb-2025 cleanup during the v1.18 rebase: removes the debug `fmt.Println`s from loadbalancers.Reconcile AND rips out the `PrivateIP` plumbing (types_class field, LBSpec field, LBSpecs wiring, defaulting) while keeping `IPAllocationMethod` and the dynamic-IP read-back. This over-removal is what 8c6bd5fa ("Fix Azure Private Cluster") reinstates 3 months later — a classic fix-of-fix pair.
- **pr/ticket context:** none.
- **parity vs v1.26.0:** N/A alone — intermediate link. NET across 3d6a0310+8c6bd5fa = keep PrivateIP feature, no debug prints, static-IP-only frontend config in spec.go.
- **resolution guidance:** Do NOT pick in isolation (it would delete the PrivateIP feature that 8c6bd5fa restores). Apply the LB unit as its NET (= fork spectro-master state). If replayed sequentially and this link conflicts, prefer skipping it and folding its only surviving effect (debug-print removal) into the NET resolution.

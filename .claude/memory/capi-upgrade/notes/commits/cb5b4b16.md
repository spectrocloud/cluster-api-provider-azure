# cb5b4b16 — PCP-6787 Fix CVEs
- **cluster:** cve-build-noise · **plan decision (r2):** SKIP
- **tickets:** PCP-6787 — "Update cluster-api images to fix cves" (Task, Resolved). Per batch3: "For azure v1.18.0-spectro-4.9.0: golang.org/x/crypto→0.52.0, x/net→0.55.0. Dep bumps only; superseded. Commits SKIPped."
- **PR:** #153 "PCP-6787 Fix CVEs" (merged 2026-05-29T07:22:29Z, no body/comments recorded)
- **behavior:** `go.mod`/`go.sum` bumps matching the ticket exactly: `golang.org/x/crypto` 0.50.0→0.52.0; `golang.org/x/net` 0.53.0→0.55.0 (indirect); plus companion `golang.org/x/{mod,text,sys,term,tools}` minor bumps (`mod` 0.34.0→0.35.0, `text` 0.36.0→0.37.0, `sys` 0.43.0→0.45.0, `term` 0.42.0→0.43.0, `tools` 0.43.0→0.44.0) needed to keep the `golang.org/x/*` module set internally consistent. Contract: close the specific x/crypto and x/net CVEs named in the ticket for the v1.18.0-spectro-4.9.0 image line.
- **pr/ticket context:** JIRA batch3 gives the exact target versions (crypto 0.52.0, net 0.55.0) matching the diff precisely — high-confidence ticket-to-diff correlation, no ambiguity.
- **parity vs v1.26.0:** SUPERSEDED — target's dependency floor is newer across the whole `golang.org/x/*` family (this cluster's own later go-version bumps to 1.26.4 imply an even newer x/* set), and Patch-G's govulncheck rescan re-validates crypto/net CVEs against whatever the new branch actually pins.
- **resolution guidance:** Confirmed touches only `go.mod`/`go.sum`. Safe to SKIP — textbook dependency-bump commit with a clean ticket match; no functional code touched, no conflict surface for a 3-way resolver.

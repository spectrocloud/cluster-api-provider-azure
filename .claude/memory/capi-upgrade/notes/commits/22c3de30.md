# 22c3de30 — PCP-6959 : updated go version & packages to fix vulnerabilities
- **cluster:** cve-build-noise · **plan decision (r2):** SKIP
- **tickets:** PCP-6959 — "Build Azure & Kube-VIP images and update in palette" (Sub-task, Resolved). Per batch3: "Image rebuild bookkeeping (commit 22c3de30 already SKIPped as go-version bump)."
- **PR:** #159 "PCP-6959 : updated go version & packages to fix vulnerabilities" (merged 2026-06-15T06:45:47Z; body: "PCP-6959 : updated go version & packages to fix vulnerabilities" — same as title, no further detail)
- **behavior:** Three-part build-infra bump, the final one in this cluster's go-version cascade: (1) `go.mod` `go 1.25.0` → `go 1.26.4`; (2) `Makefile` `BUILDER_GOLANG_VERSION ?= 1.23` → `BUILDER_GOLANG_VERSION ?= 1.26.4` — this is the actual default consumed by `Dockerfile`'s `ARG BUILDER_GOLANG_VERSION` (the "conf BUILD_VARS" the fork threads from Makefile → Dockerfile → CI); (3) `.github/workflows/spectro-release.yaml` `GO_VERSION`/`BUILDER_GOLANG_VERSION` env vars 1.26.3→1.26.4 in both build steps (continuing the cascade started by `97122011`/`ae875a02`/`d0610cb2`). Contract: keep all three build-vars surfaces (go.mod toolchain, Makefile default, CI env override) mutually consistent at whatever the current-safe Go patch version is.
- **pr/ticket context:** PR body restates the title only; batch3 JIRA explicitly flags this as go-version bump bookkeeping, already SKIPped.
- **parity vs v1.26.0:** SUPERSEDED — the target tag's own toolchain floor and Patch-G's govulncheck→bump→tidy→rebuild→rescan loop will independently arrive at whatever Go patch version is current-safe at execute time; carrying `1.26.4` specifically is unnecessary and would likely be immediately re-bumped again.
- **resolution guidance:** Confirmed touches only `.github/workflows/spectro-release.yaml`, `Makefile`, `go.mod` — no `.go` files. Safe to SKIP. This is the commit that makes explicit the Makefile↔Dockerfile build-vars link flagged under `5f44feb4` and `d0610cb2`: `BUILDER_GOLANG_VERSION` is defined once in the Makefile and threaded into the Dockerfile ARG, so the pipeline's P3c build-vars refresh only needs to re-pin this one Makefile default (plus the parallel CI env vars) at execute time to keep the whole chain consistent on the new branch — no per-commit reconciliation needed.

# 42eb65da — Added Spectro Manifests
- **cluster:** webhook-runtime-infra · **plan decision (r2):** NEEDS-DECISION (MIXED generated+code) · Regression Watch: Low
- **tickets:** none
- **PR:** none — direct push (Feb-2025 re-fork base commit)
- **behavior:** Brings in the whole Spectro manifest layer (11.7k lines): (a) `spectro/` kustomize tree — `base/` (per-cluster controller Deployment build) and `global/` (management-plane build: controller Deployment in `capi-webhook-system`, webhook Service, Mutating/ValidatingWebhookConfigurations, ASO, NMI) plus `spectro/run.sh` (`kustomize build --load-restrictor LoadRestrictionsNone {global,base} > generated/core-{global,base}.yaml`); (b) `config/default/` patch inputs referenced by those kustomizations (manager_image_patch, manager_pull_policy, manager_credentials_patch, manager_webhook_patch → containerPort 9443 + cert mount from secret `capz-webhook-service-cert`, aad-pod-identity-deployment, webhookcainjection_patch, namespace); (c) committed OUTPUTS `spectro/generated/core-{base,global}.yaml`; (d) hand-edits to 9 `config/crd/bases/*.yaml` adding a `conversion:` webhook stanza (with placeholder `namespace: default` — later fixed and re-homed by 604a2909); (e) junk: `config/default/.DS_Store`, `spectro/.DS_Store`.
- **pr/ticket context:** none; structure inferred from tree + Spec Patch F.
- **parity vs v1.26.0:** Split verdict per Patch F:
  - **Generator INPUTS = FORK-ONLY must-carry:** `spectro/base/**`, `spectro/global/**`, `spectro/run.sh`, `config/default/*.yaml` patches (upstream config/default has ONLY kustomization.yaml at both F and v1.26.0 — these files are pure fork inputs and are what palette `vendorcrd-sync sync --provider azure` consumes to regenerate `config/vendorcrd/azure`).
  - **OUTPUTS = REGEN, do not cherry-pick:** `spectro/generated/core-{base,global}.yaml` (regenerated by run.sh/vendorcrd-sync; verify via `vendorcrd-sync verify --provider azure`, NOT by SHA presence).
  - **CRD-base conversion hand-edits = DROP:** superseded by 604a2909's `spectro/global/patch_crd_webhook_conversion.yaml` (kustomize-injected, correct `capi-webhook-system` namespace). CRD bases at v1.26.0 must stay pristine `make generate` output or the T1 no-drift gate fails.
  - `.DS_Store` files: drop.
- **resolution guidance:** Human split, but mechanical: carry (a)+(b) as-is onto the new branch (take the S-tip state of these files, not this commit's state — later commits 604a2909/79c0d269/aaba2a2e/07619357 fixed them); exclude `spectro/generated/*`, CRD-base edits, and .DS_Store. Post-conditions (webhook Service targeting `webhook-server`, controller args WITHOUT `--webhook-port`, cert secret name `capz-webhook-service-cert`) are verified in the vendorcrd output at the palette-integrate stage.

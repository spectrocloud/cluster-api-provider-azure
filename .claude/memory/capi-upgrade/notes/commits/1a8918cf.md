# 1a8918cf — Spectro CICD integration II
- **cluster:** webhook-runtime-infra · **plan decision (r2):** NEEDS-DECISION (conflict on Dockerfile)
- **tickets:** none
- **PR:** none — direct push (Feb-2025 re-fork base commit)
- **behavior:** Converts Dockerfile + Makefile to the Spectro build chain. **Dockerfile:** upstream `golang:1.22` builder → palette hardened builder image (`BUILDER_GOLANG_VERSION` build-arg, alpine, `--platform=$TARGETPLATFORM`), FIPS support (`CRYPTO_LIB` → `GOEXPERIMENT=boringcrypto`, build via `go-build-fips.sh` / `go-build-static.sh`), post-build `assert-static.sh`/`assert-fips.sh`/`scan-govulncheck.sh` gates, distroless nonroot runtime. (Contained a duplicate build step later removed by 364fe27b.) **Makefile:** `GOARCH := amd64` pin, `FIPS_ENABLE`/`BUILD_ARGS`/`RELEASE_LOC`, `SPECTRO_VERSION ?= 4.0.0-dev`, `TAG ?= v1.18.0-spectro-${SPECTRO_VERSION}`, `ALL_ARCH = arm64 amd64`, `docker-build` switched to `docker buildx build --load --platform linux/${ARCH} ${BUILD_ARGS}` and manifest image patching pointed at `config/default/manager_image_patch.yaml` (the spectro input file from 42eb65da).
- **pr/ticket context:** none; behavior inferred from diff.
- **parity vs v1.26.0:** **FORK-ONLY must-carry.** Upstream v1.26.0 Dockerfile/Makefile evolved independently (newer golang builder, `TAG ?= dev`, full ALL_ARCH, docker-build targets `config/capz/manager_image_patch.yaml`) — nothing upstream provides FIPS/palette registries/govulncheck-in-image.
- **resolution guidance:** Resolve the Dockerfile conflict by taking the FORK file wholesale at its S-tip state (which already includes 364fe27b's dedup and the hardened-images builder ref `us-central1-docker.pkg.dev/palette-images-dev/hardened-images/builder/golang`), then verify nothing new in upstream's Dockerfile (new build args/ldflags conventions) is needed by v1.26.0's Makefile targets. Makefile: re-apply the fork variable block onto upstream's v1.26.0 Makefile (3-way; keep upstream target bodies where they don't touch the fork vars) and **bump `TAG ?= v1.18.0-spectro-…` → `v1.26.0-spectro-…`** (stale-version footgun; also mirrors S-tip BUILDER_GOLANG_VERSION handling — final Go version comes from the Patch-G CVE gate). Keep the manifest-patch target pointing at the spectro `config/default/manager_image_patch.yaml` input, since spectro/base consumes it.

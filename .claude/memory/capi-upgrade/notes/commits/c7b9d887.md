# c7b9d887 — add check for frontend ip
- **cluster:** networking · **plan decision (r2):** NEEDS-DECISION (conflict: azure/services/loadbalancers/loadbalancers.go)
- **tickets:** none (no ticket/PR context; behavior inferred from diff only)
- **PR:** none — direct push
- **behavior:** One-line nil guard on 601023f0's read-back: check `loadBalancer.FrontendIPConfigurations != nil` before dereferencing, preventing a panic in `loadbalancers.Reconcile` when Azure returns an LB without frontend configs.
- **pr/ticket context:** none.
- **parity vs v1.26.0:** N/A alone — hardening of fork-only code (see 601023f0). Already folded into the fork's current armnetwork-based nil-guard chain on spectro-master.
- **resolution guidance:** Absorbed by the NET state of the private-cluster LB unit. Do not resolve line-by-line against the old go-autorest types (`network.LoadBalancer`); the fork's current spectro-master loadbalancers.go already expresses this as `lbIPConfig != nil && len(lbIPConfig) > 0 && lbIPConfig[0].Properties.PrivateIPAddress != nil && *... != ""` on `armnetwork.LoadBalancer` — copy that.

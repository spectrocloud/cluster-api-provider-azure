# 79c0d269 — Manifest Fixes
- **cluster:** cloud-environments (previous-upgrade reconcile subgroup) · **plan decision (r2):** NEEDS-DECISION (MIXED; Regression Watch Low)
- **tickets:** none
- **PR:** none — direct push (2025-03-11, v1.18 rebase repair)
- **behavior:** Regenerated artifacts for the v1.18 base: `config/crd/bases/*.yaml` (all CRDs), cainjection patches, certmanager cert, `managedclusters_mock.go` mock regen, kustomization updates. Also accidentally commits `config/.DS_Store` (junk). No hand-written logic.
- **pr/ticket context:** none
- **parity vs v1.26.0:** **SUPERSEDED (REGEN).** v1.26.0 ships its own generated CRDs/manifests; regenerating on the new branch produces the correct output.
- **resolution guidance:** **REGEN, never cherry-pick**: run `make generate manifests` on `spectro-v1.26.0-master`; spectro-specific manifests come from the palette `vendorcrd-sync` stage (Spec Patch F). Do not carry `.DS_Store`. Mock regen comes from `make generate` too.

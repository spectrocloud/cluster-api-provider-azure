# f9cfc76b — Update
- **cluster:** webhook-runtime-infra · **plan decision (r2):** NEEDS-DECISION (conflict on main.go)
- **tickets:** none (follow-up to PEM-2613 commit ad0f208f, authored one day later)
- **PR:** none — direct push (Jayesh Srivastava, Aug 2023)
- **behavior:** Two fixes to make ad0f208f coexist with the newer upstream baseline: (1) **removes** the fork's `AddTLSOptions` function and its call — upstream CAPI `flags.AddManagerOptions` already registers `--tls-min-version`/`--tls-cipher-suites`, and registering them twice panics pflag at startup. Side effect: fork TLS hardening becomes non-configurable defaults (TLSMinVersion "" → component-base default TLS12). (2) **re-adds the `--metrics-bind-addr` flag** (default `localhost:8080`) and forces `metricsOptions.BindAddress = metricsAddr` over whatever `flags.GetManagerOptions` computed — upstream had replaced this flag with `--diagnostics-address`, but palette's shipped Deployment args pass `--metrics-bind-addr=localhost:8080` (present in both `spectro/generated/core-base.yaml` and `core-global.yaml`).
- **pr/ticket context:** none; behavior inferred from diff + deployment manifests.
- **parity vs v1.26.0:** **FORK-ONLY must-carry (both halves).** v1.26.0 has no `--metrics-bind-addr` flag; a manager binary without it **crashes on "unknown flag" against every existing palette deployment spec**. The TLS-flag removal stays relevant for the same duplicate-registration reason.
- **resolution guidance:** File `main.go`. Part of the 4-commit main.go unit (see cluster note). Concretely on v1.26.0: keep upstream's `flags.GetManagerOptions` call but (a) register the extra `--metrics-bind-addr` StringVar and assign `metricsOptions.BindAddress = metricsAddr` after GetManagerOptions, (b) do not register fork TLS flags. Alternative long-term cleanup (update palette deployment args to `--diagnostics-address`) is a cross-repo change — do NOT attempt it inside this upgrade; carry the flag.

# a822bc84 — fix for BET-3963
- **cluster:** webhook-runtime-infra · **plan decision (r2):** SKIP (empty on cumulative tree)
- **tickets:** BET-3963 (not in provided JIRA batches; original intent recovered from pre-replay history)
- **PR:** none — direct push (Snehal Amrutkar, Jul 2022, replayed)
- **behavior:** EMPTY commit in the replayed history (no file changes). Original BET-3963 fix (pre-replay e9fa2469f8 / 9b67ebd8e3) reordered the agent-pool reconcile in `azure/services/agentpools/agentpools.go`: the "agent pool must be in terminal provisioning state (Canceled/Failed/Succeeded)" transient-error check was moved so it only guards the actual update call, not the diff computation — preventing scale operations from being blocked/spammed while a pool is transitioning.
- **pr/ticket context:** no ticket/PR context in inputs; behavior inferred from the recovered original diff.
- **parity vs v1.26.0:** **SUPERSEDED.** The v1.3-era `agentpools.go` reconcile shape (direct SDK CreateOrUpdate with ProvisioningState string checks) no longer exists — at S and at v1.26.0 the agentpools service is the spec/ASO-based implementation with no "non terminal state" string check (`git grep "non terminal state" 0164d80e -- azure/services/agentpools/` is empty). Nothing to carry.
- **resolution guidance:** SKIP is safe and correct — empty commit, and the behavior it once patched was rewritten out from under it upstream years ago.

# 5f44feb4 — Vulnerability fix
- **cluster:** cve-build-noise · **plan decision (r2):** SKIP
- **tickets:** none (pre-JIRA-tracking fork commit)
- **PR:** none — direct push
- **behavior:** Bumps `go.mod`/`go.sum` to close CVEs current at the time (Apr 2025): `go 1.23.0`+`toolchain go1.23.6` → `go 1.23.8`; `golang.org/x/crypto` 0.35.0→0.36.0; `golang.org/x/text` 0.22.0→0.23.0; `golang.org/x/net` 0.35.0→0.38.0 (indirect); `golang.org/x/sync`/`sys`/`term` minor bumps; `golang-jwt/jwt/v4` 4.5.1→4.5.2, `/v5` 5.2.1→5.2.2; a few `// indirect` marker corrections (`Azure/azure-sdk-for-go`, `Azure/go-autorest/autorest/to`). No behavior change beyond the toolchain/dependency floor — the contract to preserve is "no known-CVE dep versions in go.sum", not any specific version pin.
- **pr/ticket context:** none — direct push, no ticket or PR context; behavior inferred from diff only.
- **parity vs v1.26.0:** SUPERSEDED — target `go.mod` sits at `go 1.25.0` (later commits in this same cluster push to 1.26.4) and carries far newer `x/crypto`/`x/net`/`x/text`/`jwt` versions; the specific CVEs this commit closed are moot once on the v1.26.0 dependency floor.
- **resolution guidance:** Safe to SKIP outright — carrying it forward would immediately be re-bumped by every later commit in this cluster and by target's own newer floor. No files outside `go.mod`/`go.sum` touched, confirmed via `git show --stat`. Note: this commit's builder-go pin only exists at the `go.mod`/toolchain level here, but the *build-time* `BUILDER_GOLANG_VERSION` (the ARG consumed by `Dockerfile` and defaulted in `Makefile`, see `22c3de30`'s Makefile diff `BUILDER_GOLANG_VERSION ?= 1.23` → later `1.26.4`) is a separate axis from `go.mod`'s `go`/`toolchain` directive. The Patch-G CVE gate (govulncheck → bump → tidy → rebuild → rescan) plus the pipeline's P3c build-vars refresh will re-derive/re-pin `BUILDER_GOLANG_VERSION` at execute time against whatever the new branch's toolchain actually requires — so this commit's builder pin doesn't need to be carried or reconciled by hand.

# 94233eeb — Merge pull request #126 from spectrocloud/PCP-5293 (controller-runtime v0.20.4)
- **cluster:** webhook-runtime-infra · **plan decision (r2):** NEEDS-DECISION (conflicts: 6 webhook files + go.mod/go.sum)
- **tickets:** PCP-5293 — fleet-wide `sigs.k8s.io/controller-runtime` v0.19.x → v0.20.4 (forced by kyverno CVEs in ally; CAPZ needed a manual bump + replace directives)
- **PR:** #126 "PCP-5293 Upgrade controller-runtime version to v0.20.4" (vishu2498, merged 2025-10-16; empty body). Note: in the replayed history this "merge" is a single-parent flattened commit carrying the whole PR.
- **behavior:** Transitional dep bump: go.mod/go.sum to controller-runtime v0.20.4 (go 1.23.8 era) plus the mechanical webhook-interface migration it forces — 6 files (`api/v1beta1/azure{cluster,clusteridentity,clustertemplate,managedcluster,managedclustertemplate}_webhook.go`, `exp/api/v1beta1/azuremachinepoolmachine_webhook.go`) move from `webhook.Validator`/`webhook.Defaulter` (methods on the type, no ctx) to `webhook.CustomValidator`/`CustomDefaulter` (`WithValidator(...)`, ctx + runtime.Object signatures). No functional validation change — signatures only.
- **pr/ticket context:** JIRA: fork bumps are transitional; "upstream version jump needed for CAPI/GCP/OpenStack"; after RC releases palette pins new images.
- **parity vs v1.26.0:** **SUPERSEDED — drop entirely. Verified with evidence:** (1) `git show v1.26.0:go.mod` → `sigs.k8s.io/controller-runtime v0.23.3`, `go 1.25.0` — well past v0.20.4; (2) the 6 conflicted files DO NOT EXIST at v1.26.0 — `git ls-tree v1.26.0 api/v1beta1/ | grep webhook` is empty; all webhooks live in `internal/webhooks/` + `internal/exp/webhooks/` and already implement `webhook.CustomValidator`/`CustomDefaulter` (e.g. `internal/webhooks/azurecluster_webhook.go` lines 50-61). Both the dep bump and the interface migration are fully contained in the target.
- **resolution guidance:** SKIP/drop at execute — do not attempt conflict resolution; there is nothing fork-specific inside. Add to `skip-commits.txt` (or known-commits.tsv decision=SKIP, reason "superseded by v1.26.0: cr v0.23.3 + internal/webhooks CustomValidator migration"). Recommend flipping NEEDS-DECISION → SKIP in the plan; this removes 8 conflict files from the human shortlist at zero risk.

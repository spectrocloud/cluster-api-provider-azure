# ee19ef20 — Removed Dockerfile and Makefile
- **cluster:** cloud-environments (#117 chain, root) · **plan decision (r2):** NEEDS-DECISION (conflicts on 8 files incl. defaults.go, cluster.go, identity.go, managedcontrolplane.go, loadbalancers.go)
- **tickets:** PEM-7511 — Azure Secret Cloud support in palette (deploy clusters inside AzSecret region); umbrella PEM-7501
- **PR:** #117 "Pem 7511 azure secret cloud support in palette" (author YinzheZhang/HigashikataZhangsuke)
- **behavior:** Misleading subject — this is the **root feature commit of the AzureSecret chain** (17 files, +789), named for having dropped Dockerfile/Makefile edits from the branch diff. Introduces: dynamic env→SDKv2 `cloud.Configuration` conversion in `defaults.go` (+116, `getCloudConfigurationFromEnvironment` incl. Graph/Gallery/Storage endpoints); custom-CA transport start in `azure_secret_cloud.go` (+44); Cloud config + transport plumbing in `identity.go` (+204); `DisablePrivateDNSAnnotation` bypass + `getHostName()` in `cluster.go`; kubeconfig CA-injection + debug logging in `managedclusters.go` (+62) and `azureasomanagedcontrolplane_controller.go`; **cross-RG duplicate VNet-link prevention** in privatedns (`ListAllZonesByName`, `ListByZone`, skip-if-already-linked, +110 over 4 files); custom transport in `util/remote/client.go` (+68, later fully reverted by a0e29487/1b4fdec4/b7f8d03b).
- **pr/ticket context:** PR body links a design doc; reviewers (vishu2498, snehala27, guyni) pushed on: debug prints → leveled logs, no fallback cert path, constants for annotation keys, keep behavior public-cloud-safe, comment the link_reconciler logic. Author confirmed the dup-link case came from a real test failure ("accidental duplication caused error").
- **parity vs v1.26.0:** **FORK-ONLY must-carry (net form).** None of this exists upstream. Two carve-outs: (a) identity.go Cloud config for the client-secret credential IS upstream in v1.26.0 (PARTIAL overlap); (b) the util/remote changes are net-zero at S — do not carry.
- **resolution guidance:** Do NOT replay the chain commit-by-commit — it contains reversal pairs and a conflict-resolution merge (2071dd28, see cluster summary). Apply the **net #117 diff** (`git diff 7af8521e57^1 7af8521e57`) adjusted to v1.26.0, or rebuild per-file from S tip. Preserve: privatedns dup-link logic, defaults.go env conversion + `GlobalHTTPClient` injection, identity.go transport/DisableInstanceDiscovery extras, controllers CA injection, cluster.go annotation bypass.

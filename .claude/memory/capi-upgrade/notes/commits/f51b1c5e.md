# f51b1c5e — Additional Changes & Fixes
- **cluster:** cloud-environments (previous-upgrade reconcile subgroup) · **plan decision (r2):** NEEDS-DECISION (MIXED; Regression Watch HIGH)
- **tickets:** none
- **PR:** none — direct push (Vishwanath Taykhande, 2025-03-10, during the v1.11→v1.18 fork rebase)
- **behavior:** NOT a feature. Build-repair megacommit from the **previous** upgrade: after old fork commits were replayed onto the v1.18.0 base (committer date 2025-02-21), this fixes the damage — deletes stale `azure/services/managedclusters/client.go` (216 lines, pre-ASO API that doesn't exist in v1.18), restores v1.18-form `managedclusters/{managedclusters,spec}.go` and `scalesets.go`, restores go.mod/go.sum, fixes `UpgradeChannel` pointer/type mismatches (infrav1exp→infrav1) in managedcontrolplane.go, removes duplicated fork methods (SetAutoUpgradeVersionStatus etc. existed twice), fixes stale main.go imports/manager options (v1alpha4-era fields), re-adds exp/ webhook+test files (wrongly; deleted again by aa405349), adds `util/maps.FilterByKeyPrefix` (dead code at S tip — zero call sites; not in v1.26.0).
- **pr/ticket context:** none
- **parity vs v1.26.0:** **SUPERSEDED / NOT-APPLICABLE.** Every repair is specific to the 2025-02 replay-onto-v1.18; v1.26.0 already has the correct upstream state for all restored files. The only net-new surviving content (FilterByKeyPrefix) is unused.
- **resolution guidance:** **SKIP for the v1.26 rehearsal.** Do not replay repair commits of a previous rebase — the equivalent repairs for THIS rebase come from resolving each feature commit against the v1.26 tree (executor build-to-green loop). Downgrade the Regression Watch HIGH: risk is covered by Tier-1 build+`make generate` no-drift on the new branch, not by carrying this commit. If any hunk conflicts get force-picked, drop the exp/ file additions and the go.mod/go.sum hunks unconditionally.

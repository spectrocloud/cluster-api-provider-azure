# debee817 — Fixed osType issue
- **cluster:** aks-managed-clusters · **plan decision (r2):** NEEDS-DECISION
- **tickets:** none
- **PR:** none — direct push (Snehal Amrutkar, 2022-09-12)
- **behavior:** Follow-up to 15a9f3a5: make `osType` (and name/osDiskSizeGB/sku/providerIDList) available in the **v1alpha4** AMMP CRD schema so pre-v1beta1 Palette clusters could round-trip osType during conversion. Rebased remnant is generated-file-only: +93 lines in `config/crd/bases/infrastructure.cluster.x-k8s.io_azuremanagedmachinepools.yaml` (a v1alpha4 schema section) + two empty files.
- **pr/ticket context:** no ticket/PR context; behavior inferred from diff only.
- **parity vs v1.26.0:** SUPERSEDED. v1alpha3/v1alpha4 APIs no longer exist in v1.26.0 (or in the fork's v1.18 base); `osType` is first-class in the v1beta1 AMMP CRD upstream. CRD yaml is regenerated by `make generate/manifests` and vendorcrd-sync.
- **resolution guidance:** SKIP / REGEN. Nothing to carry — the CRD base yaml at v1.26.0 is produced by controller-gen from upstream types that already include osType.

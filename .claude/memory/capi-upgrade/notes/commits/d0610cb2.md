# d0610cb2 — PCP-6734 Update go version for build
- **cluster:** cve-build-noise · **plan decision (r2):** SKIP
- **tickets:** PCP-6734 — "Update go version (1.26.3) and rebuild images" (Task, Resolved). Per batch3: "Builder-image/go bump across CAPx incl. azure v1.18.0-spectro-4.8.4. Build-infra only; superseded by target go + Patch-G gate. Commits SKIPped."
- **PR:** #150 "PCP-6734 Update go version for build" (merged 2026-05-19T06:03:58Z, no body/comments recorded)
- **behavior:** Four-part build-infra bump: (1) `go.mod` `go 1.24.0`+`toolchain go1.24.3` → `go 1.25.0`; (2) `Dockerfile` base image for the `toolchain` build stage changes registry from `us-docker.pkg.dev/palette-images/build-base-images/golang:${BUILDER_GOLANG_VERSION}-alpine` to `us-central1-docker.pkg.dev/palette-images-dev/hardened-images/builder/golang:${BUILDER_GOLANG_VERSION}-alpine` (moves onto Spectro's hardened-image builder registry, same `BUILDER_GOLANG_VERSION` ARG); (3)+(4) `go.mod`/`go.sum` dependency bumps and `.github/workflows/spectro-release.yaml` version-string bump (consistent with the `GO_VERSION`/`BUILDER_GOLANG_VERSION` env vars established by `97122011`/`ae875a02`). Contract: keep the builder toolchain and go.mod floor in lockstep and sourced from Spectro's hardened base image.
- **pr/ticket context:** ticket ties this to "1.26.3" rebuild bookkeeping across the whole CAPx fleet; batch3 JIRA explicitly calls it build-infra-only and already-SKIPped.
- **parity vs v1.26.0:** SUPERSEDED — target's own `go 1.25.0`+ floor (this cluster's own later commit `22c3de30` shows the fork itself moving to `go 1.26.4`) makes the specific `1.25.0` pin moot; the hardened-registry `Dockerfile` FROM-line change is a Spectro build-infra convention, not upstream-observable behavior, and will be re-derived by whatever base image the new branch's Dockerfile already uses (post-execute the Dockerfile is regenerated/carried forward as part of the fork's own build tooling, not reconciled against upstream since upstream's Dockerfile differs entirely in registry).
- **resolution guidance:** Confirmed touches only `.github/workflows/spectro-release.yaml`, `Dockerfile`, `go.mod`, `go.sum` — no `.go` files. Safe to SKIP: the go-version/toolchain floor is superseded by target + Patch-G CVE gate re-derivation. Note the interaction flagged for `5f44feb4`: this is the second of the two build-vars axes (Dockerfile `ARG BUILDER_GOLANG_VERSION` consumed from Makefile's `BUILDER_GOLANG_VERSION ?=` default, see `22c3de30`) — the P3c build-vars refresh at execute time re-pins this default against the new branch's actual toolchain requirement, so this commit's specific `1.25.0`/registry-path pin doesn't need manual reconciliation.

# 604a2909 — Fix Spectro Manifest Generation Script (#131)
- **cluster:** webhook-runtime-infra · **plan decision (r2):** NEEDS-DECISION (MIXED generated+code) · Regression Watch: Low
- **tickets:** none on commit
- **PR:** #131 "Fix Spectro Manifest Generation Script" (vishu2498, merged 2026-04-09; no body/comments). Second squashed message line: "Adding support for AKS cilium CNI".
- **behavior:** Overhauls the spectro manifest generation so `spectro/run.sh` output is correct on the v1.18-era tree, in three distinguishable parts:
  1. **Generator inputs (config-as-code):** `spectro/base/kustomization.yaml` now also builds `../../config/rbac` and JSON6902-patches `base-manager-role` (`patch_base_manager_role.yaml`: +CRD get/list/watch, rules[5].resources→'*'); `spectro/global/kustomization.yaml` rewritten (adds `webhook_rbac.yaml`, ASO patches `patch_aso_controller_manager/settings/metrics_service/webhook_annotations`, drops the 9× per-CRD `patch_crd_webhook_namespace` blocks); NEW `patch_crd_webhook_conversion.yaml` — injects the CRD `conversion:` stanza (service `capz-webhook-service` @ `capi-webhook-system`, path `/convert`) via kustomize instead of hand-edits.
  2. **De-hand-editing generated files:** removes the conversion stanzas 42eb65da had hand-planted in `config/crd/bases/*.yaml` and refreshes those CRDs toward real controller-gen output; regenerates `spectro/generated/core-{base,global}.yaml`.
  3. **Webhook disablement (the cilium part):** deletes 4 entries from `config/webhook/manifests.yaml`: `default.azuremanagedcontrolplanes`, `default.azuremanagedmachinepools`, `validation.azuremanagedcontrolplanes`, `validation.azuremanagedmachinepools` — i.e. palette wants AMCP/AMMP admission webhooks OFF (defaulting would fight palette-managed AKS fields, validation rejects cilium/`networkPluginMode` combinations palette sets; related fork commit 0cb06ceb "Validation disabled for AzureManagedControlPlane" in another cluster).
- **pr/ticket context:** PR title only; cilium intent from squash message; no review comments available.
- **parity vs v1.26.0:** **MIXED.** Parts 1 (inputs) = FORK-ONLY must-carry. Part 2 = REGEN (make generate + run.sh/vendorcrd-sync reproduce). Part 3 = FORK-ONLY behavior but **implemented fragilely**: `config/webhook/manifests.yaml` is controller-gen output — on the v1.26.0 branch `make generate` regenerates it from the (still present) kubebuilder markers in `internal/webhooks/`, restoring all 4 entries and failing the T1 no-drift gate, or worse, silently re-enabling the webhooks.
- **resolution guidance:** ⚠ **Known inconsistency to resolve, not preserve blindly:** at S, `spectro/generated/core-global.yaml` STILL contains the 4 removed webhook entries (lines ~77862, ~84667 under capz-mutating/validating-webhook-configuration) — generated output was never rebuilt after the manifests.yaml edit, so the shipped palette global manifest still registers AMCP/AMMP webhooks while `config/webhook/manifests.yaml` says they're gone. Decide the intended end state with the fork owner (are AMCP/AMMP webhooks supposed to be live in palette global clusters? The in-code validation relaxations of 0cb06ceb/e3c6f868 suggest they ARE served today). Then implement removal (if desired) as a **spectro/global kustomize delete-patch on the webhook configurations** (survives regeneration), never as edits to generated files. Carry part-1 files at S-tip state; regenerate everything in part 2; verify via `vendorcrd-sync verify --provider azure` post-conditions, not SHA presence.
